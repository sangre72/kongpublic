# 서울드라이브 작업 규칙 (MUST — u_5192~5197, 2026-09-16)

## 1. 순서: 룰 먼저, 학습은 그 다음 (u_5192)
> "주행 화면 보면서 강남역에서 시청역까지 추돌이나 충돌 없이 주행 잘 하게
>  우선 룰을 계속 개선해. 그리고 그다음에 학습데이터를 모으자"

교사(GEOM/teacher.js)가 사고 없이 완주할 때까지 수집하지 않는다.
BC 는 교사를 복제할 뿐이므로, 교사가 못 하는 걸 모델이 할 수는 없다.

## 2. 쓰레기 데이터 금지 (u_5197)
룰이 미완인 상태에서 모은 데이터는 쌓지 않는다.
실제로 23만 프레임(속도 0.2~0.3 인 정지 데이터)을 전량 삭제한 적이 있다.
수집 전 반드시 교사 품질을 먼저 측정한다.

## 3. 이벤트마다 텔레그램 보고 (u_5193/5195 — 절대 규칙)
> "문제가 생길때, 수정 할때, 개선될때 마다 나한테 텔레그램을 보내도록해"
> "밤새서라도 계속 이벤트시에 하는것 절대 규칙이니 잊지말고"

보고 시점: 문제 발견 / 원인 규명 / 수정 / 개선 확인 / 실패·되돌림.
침묵하고 오래 작업하지 않는다. 실패도 즉시 보고한다.

## 4. 화면을 계속 본다 (u_5196)
> "넌 화면을 계속 모니터링해야해. 한문제 해결할때마다 수시로"

`screencapture -x -R0,25,763,762 /tmp/x.png` 후 Read 로 확인.
숫자만 보다 놓친 사례: 안내선이 점선 위에 얹힌 것, 경로가 유턴하는 것 —
둘 다 오너가 화면으로 먼저 발견했고, 내 숫자 검증은 함수 반환값을
계산한 것이라 못 잡았다.

## 5. 도로교통법 준수가 룰의 일부 (u_5194)
> "도로 교통법 회전 직진 후진 유턴시 다 잘 지키는것도 룰이야"

- 차로: 점선과 점선 사이(차로 중앙)로 주행. 차선을 물면 위반.
- 회전: 좌회전은 좌측 차로, 우회전은 우측 차로에서. 준비 거리 =
  옮길 차로수 × 속도 × 3초(한 차로 변경에 3초).
- 차선변경: 한 번에 한 칸씩. 두세 칸 동시 변경은 없다.
- 중앙선 침범·역주행 금지.

## 측정 원칙 (반복해서 틀렸던 것)
- 도로 절대좌표로 잰다: `g2.lat + roadW/2`. `cross(ct)` 는 교사 목표 기준이라
  목표가 틀리면 0 이 나온다(이것 때문에 "87% 유지"로 오보고했다).
- 표본 100초 이상. 20초에서 0% 였던 게 100초에 48% 였다.
- 차가 실제로 움직이는 상태에서 잰다. 교사만 켜면 차가 안 움직여
  정지 상태 수치가 나온다.
