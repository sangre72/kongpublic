RECIPE: kong3d_engine_ops
★USE-FIRST measurement toolbox (real-subject modeling, priority order):
 1. official spec ratios (L/H/W, wheelbase) — numbers over pixels.
 2. computed silhouette trace: PIL threshold ref → per-column contour → corner-detect → GUI-click.
 3. computed landmark→world: measure ref px → body-frac(u,v) → z=(0.5-u)*W, y=H*(1-v); TYPE exact into inspector.
 4. circle-fit for wheels/arches from exported profile pts.
 5. ★SHAPE-FROM-SHADING (a_4250, PROVEN): scripts/shape_from_shading.py <ref> <out> — luminance-gradient(curvature dir) + Laplacian value-STEP(=crease/panel-edge, finds hood-V exactly) + specular ridge(=shoulder char-line) + value-band segmentation(=part regions). Outputs crease_polyline_frac + specular_ridge_frac + regions(image-frac) + overlay.png(verify edges land on features). Convert fracs→world via item-3 formula. Clean-bg refs=auto; garage-photo bg=specular noise lands off-body(ignore, body edges still clean). Verified on front/side/34: front edges trace HL-trapezoids+hood-crease+intake+windshield-base; side traces belt char-line+DLO+door-cut.
GATE: verified 2026-09-07 a_4217 (worker1, all steps live-operated via kongtrol)
REG: kong3d/ (localhost:8300) — Three.js GUI game/modeling engine + KongPoko platformer
WHY: engine built to be kongtrol-operable; these are the verified operation patterns.

SERVE: cd kong3d && python3 -m http.server 8300. pages: / (editor+FPS), /ponpoko.html (platformer).
A11Y: Chrome DOM buttons fully a11y-visible (grep labels: 박스 추가/구 추가/플레이 모드/장면 저장/장면 불러오기/배경음악 추가/스폰). canvas content = screenshot only.
★COORD: header-button coords SHIFT when buttons added — re-query a11y EVERY session (incident: stale Play coord hit Save after BGM/Spawn btns were added).
★TAB: extension(Trust Wallet) can hijack-open its own tab on Chrome open — check AXRadioButton tab rows, close+switch before operating.
EDITOR: add via toolbar btns; select via object-list AXButton rows(right panel); props number-fields: click CENTER of field (a11y rect can be ~30px off vs visual — zoom-crop verify focus-ring first), then arrow-keys up/down (step increments, RELIABLE) — cmd+a + typed text unreliable in these fields; target checkbox verify checked-state via zoom-crop (a11y compact doesn't show value).
GIZMO: canvas has no a11y — estimate axis-shaft point from zoom-crop, `input drag` along axis dir. verify via props value change (e.g. pos.x 1.57→2.99).
PLAY-MODE(FPS): 플레이 모드 btn → pointer-lock. scripted input: TAP-IMPULSE friendly (W/A/S/D = 0.12/tap + held-frame velocity, space = 300ms jump-request window) because CGEvent taps can't hold keys. HUD top-left `pos x,y,z ·ground/air ·♪ctx/lastSfx ·bgm` — read via screencapture -R crop. mouse-look NOT drivable via CGEvent abs moves (pointer-lock needs relative deltas). shoot = click while locked (crosshair ray).
PONPOKO(/ponpoko.html): A/D move (~0.14/tap), W/S ladder, space jump. dbg line top-left `p x,y state ♪sfx` = feedback loop for scripted play: batch taps → crop dbg → adjust next batch. verified full stage-clear route in ar_4217 (D4 W28 D39 W24 A22 D42 W28 D8 A28 D74 S30 A8).
AUDIO: all SFX procedural (WebAudio synth buffers) — zero asset files. BGM = file-picker: 배경음악 btn → NSOpen panel → type "/" (opens go-to-path sheet) → type full path → enter → enter. verified w/ ffmpeg-generated wav; persists in scene JSON as dataURL.
RECORD: per kaymaps/_common/RECIPE_demo_video_recording.txt — window rect via CGWindowList/Quartz (re-query always), `screencapture -v -V <sec> -R<rect>`; mid-recording `screencapture -x` crops DON'T disturb the video capture (verified) → use for feedback-driven gameplay while recording. transcode telegram-size: ffmpeg scale=1408 crf26 (~3MB/90s).
VERIFY: HUD/dbg readouts + a11y object-list rows + frame-extract from recordings (recipe §4).
LIMIT: typed-entry into number fields flaky (use arrow keys); pointer-lock mouse-look unverifiable via kongtrol.
UPDATE 2026-09-07(a_4218 refactor): engine core extracted → kong3d/engine.js (createApp bootstrap, AudioSystem(SFX-recipe-table/positional/BGM), KeyInput(tap-impulse), Physics(landOnPlatforms/pushOutAABB), makePrimitive/serializeObjects/applyObjectData, canvasTexture). app.js(editor) AND game.js(KongPoko) both import './engine.js' — game = ponpoko_level1.json(level data) + game-specific visuals/logic on engine APIs. verified live post-refactor(editor add+FPS-walk, ponpoko move+ground-physics). New games: copy game.js pattern — createApp+AudioSystem+KeyInput+Physics + own level JSON.
UPDATE 2026-09-07(a_4219 GUI-authoring): editor entity palette(발판/사다리/과일/뱀/스폰 role-tagged) + inspector AI(behavior select static/patrol/chase, speed, range) + PHYSICS(gravity/collider checkbox, bounce) sections — all saved in scene JSON, game honors(levelFromScene maps roles; snake ai, platform collider-skip, fruit gravity+bounce). GUI-AUTHOR WORKFLOW(verified, recorded kong3d_gui_authoring.mp4 7min): Clear→palette-click add→pos via inspector field click+arrow-keys(pos row fields visual centers ~x1522/1587/1652 @y665; AI speed/range ~x1585 @y850/884; behavior select ~1585,818→option rows +~25px/row; PHYS checkboxes a11y-corner ~1508,950/975; bounce ~1590,1002)→Save(장면 저장)→game tab reload=GUI STAGE(localStorage kong3d_scene, role-tagged→level; fallback bundled JSON)→play→design-iterate back in editor(select in object-list→tweak→Save→game reload). Checkbox/select clicks: a11y-corner works, +35px-off visual estimates don't — trust a11y for DOM controls, zoom-crop only to confirm state.
UPDATE 2026-09-08(a_4220 M4, COMPUTED-TRACE method — compute-vs-AI-split): VLM eye = coarse at exact-pixel outline work. Precise GUI tracing = (1) PIL threshold(<110) on ref jpg → per-column topmost-dark = silhouette contour; corner-detect via slope-delta(±25px window, >14°) (2) map src-px→viewport click coords via ref-plane screen rect measured from screenshot(gray-value scan for plane bbox) + REFIT against 2 GUI-verified landmark points(single-measure rect drifts ~17px at far edge — always refit) (3) kongtrol clicks those coords into the ✎ curve tool, zoom-crop verify per checkpoint(markers ON line), ∠ toggle between corner/smooth runs — VERIFY marker COLOR in crop(pink=corner/orange=smooth) to catch toggle desync(recurred 2x: first CK sometimes misses right after mode-enter — always crop-check first marker's color).
UPDATE 2026-09-08(a_4242 M5): WIDTH PROFILE inspector section(profile objects, non-lathe) = beltY/roofW/rockW number fields → engine applyWidthProfile(z-scale per y, ease-in tuck above belt) = tumblehome/plan-taper, saved in scene JSON, rebuild-on-change. Wheel-arch outline = computed-trace extension: arch arcs synthesized as semicircles at wheel centers(src-px from car-span ppm: center±wheelbase/2, r≈0.46m), spliced into bottom outline as corner-entry/exit + 3 smooth arc pts each — no new tool needed, plain ✎ curve points. Verified: front-view greenhouse narrowing + arch cutouts w/ wheels seated.
UPDATE 2026-09-08(M6): wheel-house exact-fit method = ⬇Export scene JSON → read profile arch entry/apex pts → circle-fit(center/r) → set wheel pos/scale via inspector to fitted values(¬eyeball). This run: traced arches ended up r0.5/cy0.45 (taller than photo-real — screen-mapping y-scale on synthesized arc pts) — wheels dia0.8 y0.4 fit them. Detail parts(DRL/tail-bar/mirrors/window-bands) = plain boxes via inspector arrows; LIMIT: 0.1-step arrows too coarse for flush-surface trim(window bands invisible inside tumblehome surface), palette auto-colors ¬semantic(no GUI color set yet — macOS picker unautomated).
UPDATE 2026-09-08(M6fix, 3/4-verify discipline): ortho-views passing ≠ done — Z-DEPTH errors only show at 3/4(user-caught): wheels z must sit inside body half-width minus tire-half (z±0.7 w/ width0.3 vs rocker half-width 0.92), uniform across all 4(one odd z = disc-pasted-look); arch see-through fixed w/ interior liner boxes(watch liner y — bottom must stay above rocker else sliver pokes below); 'floating cube on hood' was a window-band TIP piercing the cowl(long thin boxes: check both ENDS against surface, not just center). Object-list scroll: click a row then arrow-keys scrolls the overflow list(rows below fold reachable); viewport-click selects only front-most(raycast) — hidden interior parts need list selection. VERIFY AT 3/4 with zoom-crops per region before reporting.
UPDATE 2026-09-08(M6fix2 — coin-wheel root cause): flat-disc look at 3/4 = (a) z-tuck parallax(wheel 0.22 inside body wall → projected offset vs arch opening at 3/4; reduce tuck to ~0.17, z±0.75) + (b) bare cylinder flat cap(no rim cue) → FIX = hub cylinder per visible wheel(r0.45-of-tire-ratio, width slightly proud, contrasting color) = instant tire+rim read. pos/scale inspector steps changed 0.1→0.05(code) for fine placement. ★EVIDENCE RULE(orch, standing): any 'fixed' claim ships zoom-crop files demo_recordings/evidence_*.png at the failing angle — verify by viewing the crops BEFORE reporting; camera preset MUST be re-clicked before evidence crops(edits move camera; stale-frame crops = wrong region).
UPDATE 2026-09-08(M7): HEX-COLOR text field added to inspector(색상 HEX @~1585,790) — cmd+a+type+enter sets material color reliably(macOS picker bypassed); colors persist via scene JSON. Detailing learnings: (a) proud-surface rule — trim/glass bands must clear ACTUAL surface(test-высок z then walk in; calc from widthProfile unreliable, use visibility test z1.2→walk-down) (b) camera damping: after view-preset click WAIT 2s+ before evidence crops(drift = wrong crop regions; recompute regions from a fresh full-shot each session) (c) long slabs(rocker) end-caps poke into arch openings — size BETWEEN arch entries (d) DRL/fascia parts: front face is sloped; y-drop + x-out both needed to sit on fascia.
UPDATE 2026-09-08(M8 front-fidelity): NEW plan-taper params in applyWidthProfile(noseFrac/tailFrac/taperStart) → width eases toward ends = rounded plan corners + sloped nose(kills vertical slab face). ★cache: importmap module engine.js caches hard — after engine edit, load page w/ ?v=N query(cmd+r alone served stale, taper looked no-op until cache-bust). Front-fidelity moves: nose taper 0.52, headlights=strips rot-y ±10 wrapping corners(z reattach after taper narrows body), hood V-crease = 2 thin long boxes rot-y ±10 meeting at center, windshield plate y+, mirrors z-out±0.05. Verify front-view AND 3/4 each item; front-view catches slab/flush that 3/4 hides.
UPDATE 2026-09-08(M8computed): EXACT-COORD placement(no eyeball) — measure ref-photo landmark px→body-frac(u,v)→world: z=(0.5-u)*W, y=H*(1-v). Headlight trapezoid corners, V-crease apex/ends all computed this way then TYPED into inspector fields(not arrowed). ★kongtrol input text negatives need `--yes -- "-2.35"`(bare "-2.35" parsed as flag → error); number fields DO accept cmd+a+text+enter(same as hex). V-crease box from 2 endpoints: center=mid, scaleX=hypot(dz,dy), rotY=atan2(dz,dx)-offset. Front-ref(garage photo) had cluttered bg → auto-threshold failed, used manual px read on 800px downscale — for clean-bg refs PIL threshold works(side blueprint), for photos read px landmarks by eye ONCE then compute.
UPDATE 2026-09-08(a_4269 paint-app): kong3d/paint.html = mouse-driven paint page for repro-by-painting. tools(all GUI, kongtrol-drivable): hex color field+Set, brush-size slider(x74-190 → size2-80; sky-fill 45-50, tree 25-30, detail 5-12 per task), Bucket(flood-fill tol40 — LEAKS through open outlines, use only on enclosed), ★Rect Fill(drag box, fills inside only, no leak — USE for sky/horizon/ground bands), cursor coord readout(canvas x,y — solves screen↔canvas mapping: canvas origin≈screen(238,191) 1:1 this window), localStorage persistence(save per op, restore on load — reload resets tool→brush but KEEPS canvas). PAINT-ORDER that worked: rect-fill bands top-down(sky/horizon/river/grass)→brush tree masses over→small-brush grass/water texture. ★drag cadence 0.35-0.5s(faster drops mousedowns). subject must be user-own/non-IP(declined One Piece cover a_4262).
UPDATE 2026-09-09(a_4273 curved-stroke toon): stroke_plan.py = brushwork not dots — (1) FILLS as broad DIRECTIONAL sweeps per posterized color region: stroke dir follows form(foliage=downward canopy arc, grass=upward sweep, sky=near-flat), each stroke=3-pt curved polyline. (2) OUTLINES = trace Laplacian edges into ORDERED chains(8-neighbour walk), draw as connected multi-drag polylines(shared endpoints=continuous curve, ¬dots). stroke_driver.sh chains kongtrol 2-pt drags through each polyline's points w/ 0.03s gap. 681 strokes/2283 pts. cel look=14-color quantize + bold #161616 outline pass on top. NO dot-tiling.
UPDATE 2026-09-09(a_4274 verify-per-layer + Brush btn): paint.html now has explicit BRUSH button(deterministic tool switch — bucket/rect were sticky, reload's tool-reset unreliable via a11y). ★page reload gotcha: cmd+r / cmd+shift+r did NOT re-fetch edited HTML(cached DOM) — must navigate to ?v=<ts> cache-buster URL to load HTML edits(localStorage restores canvas across it). VERIFY-PER-LAYER protocol(observation-compensations item7): paint 1 color zone→screenshot→compare ref same region→fix→next. layer report line each. done: sky(blue #8fb0d6 ok), clouds(#eef2f6 fixed after brush-mode restored), dark-trees(#2f4230 ok). 
UPDATE 2026-09-09(a_4276 session-keyed persistence): paint.html localStorage KEY now = kongpaint_state_<?session= param|default> → dup tabs w/ different session ids NEVER clobber each other's canvas(root fix for the 7-tab overlay mess). fresh isolated painting = open Cmd+T → localhost:8300/paint.html?session=<ts>&clear=1. ★tab-close/blank via kongtrol UNRELIABLE(close-x offset drifts as tabs reflow; Cmd+W + address-bar-nav both failed to take) — but session-keying makes closing UNNECESSARY(stale tabs harmless, own keys). don't fight tab-close; isolate via session param instead.
UPDATE 2026-09-09(a_4277 ★DRIVER-STALL ROOT CAUSE = WINDOW-RESIZE COORD DRIFT, not BrokenPipe): repeated "driver dies/count=0/canvas stuck at sky+tree-fragments" was NOT a dead process — process was alive full duration. Real cause: Chrome window had been resized(874x670) so the driver's hardcoded canvas-origin(238,191)+1:1 scale mapped every drag OFF-canvas → nothing painted, snapshot showed stale earlier state. FIX PROTOCOL every paint session: (1) osascript set front-window bounds to a FIXED large size(e.g. {0,39,1500,954}) so full 1000x740 canvas fits on screen. (2) CALIBRATE canvas→screen fresh via the page's own cursor readout: `input drag (X-40) Y X Y`(real move so mousemove fires; src==dst drag does NOT update readout) at ≥2 screen pts, read canvas x,y from a11y, solve linear map. This window: sx=144.7+cx*1.117, sy=126.1+cy*1.119. (3) SMOKE-TEST 1 stroke(bright color, mid-canvas) + screencapture BEFORE the 6000-stroke run. Toolbar coords at this bounds: 색상입력(72,172) 색상적용(72,204) slider(72,309) 브러시(72,365) 지우기(72,483).
UPDATE 2026-09-09(a_4277 slider calibration): brush-size slider value is NON-linear vs my assumed track — MEASURE by dragging to x and screencap-cropping the sizeVal div(-R0,320,60,22). This window: value≈8+(x-30)*0.79 → invert x=30+(px-8)*1.266(size5→x26, size40→x70). Prior run's outline pass came out as THICK BLACK BLOBS because size landed 22 not 5(bad slider map) — always verify the outline size crop before trusting.
UPDATE 2026-09-09(a_4277 cel driver = single Python, no shell pipe): kong3d/scripts/cel_run.py reads cel_plan.json(canvas coords), applies calibrated map, calls kongtrol directly via subprocess(NO `python|while read` — that pipe broke=earlier stalls). fills(7 flat zones, size40) then treeline outline(size5). cel_plan.py contour extractor REWRITTEN: trace sky↔non-sky boundary(bottom sky pixel per column) = ONE meaningful silhouette treeline(4 clean segs), replaced scattered Laplacian internal-edge ticks(which looked like random dark scribbles). RESULT: clean cel landscape matching ref_4264 composition(converging tree-walls/clouds/valley/bank/foreground) + thin continuous treeline outline. GAP: river channel absorbed into greens at 200x150 classify res(no water zone in palette) — narrow water lost at that scale.
