RECIPE: title_only_regenerate (TENTATIVE — verified_runs=1)
GATE: 처음 다루는 UI요소 아님(재확인 사례) — ar_2009(2026-08-21) 1회 완전성공, 재사용 검증 필요.
REG: CineBot(localhost:4000) 포춘 탭. 날짜변경 후 YouTube제목/해시태그가 이전 날짜로 캐시되는 경우.
WHY: 날짜 A→B로 바꾸고 "운세 생성" 재실행해도 본문(12개 항목 내용)은 B일자로 정상 갱신되지만, 화면 하단 "YouTube 제목 + 해시태그" 섹션의 1~12번 제목·부제목은 이전 A일자 그대로 stale 남는 캐시버그(ar_2006/2009 실증, u_2009 유저 직접발견).

STEPS:
1. 증상 확인: 스크롤해서 "YouTube 제목 + 해시태그" 섹션 12개 항목 제목이 실제 선택된 날짜와 일치하는지 스크린샷 육안 확인(zoom-crop 아닌 전체화면).
2. 1차 시도(효과없음 확인됨): 앱 상단 "새로고침" 버튼(CineBot 앱 내부, 브라우저 새로고침 아님) 클릭 — 이것만으로는 제목 갱신 안 됨(ar_2009 STEP1 실증, 재시도 불필요).
3. 2차 시도(효과있음): "YouTube 제목 + 해시태그" 섹션 우측 "🔄 재생성" 버튼 클릭.
   ★좌표는 스크롤위치 의존 — 매번 `kongtrol see --a11y --compact | grep -i "재생성"`로 재탐색(하드코딩 금지). ar_2009 실측 예시좌표: @(1839,1104)(스크롤 상태별 상이).
4. ★타겟 확인(유저 지적, u_2009 중요): "재생성" 버튼은 12개 항목 **전체(본문+제목)**를 다시 생성한다. 제목만 문제일 때 전체 재생성은 낭비이므로:
   - 먼저 `kongtrol see --a11y | grep -i "제목.*재생성\|타이틀.*재생성"`로 제목-only 옵션이 UI에 별도 존재하는지 탐색.
   - 있으면 그것 사용(본문 유지, 제목만 갱신 — 더 빠름·안전).
   - 없으면(현재까지 확인된 CineBot 버전엔 없음, 2026-08-21 기준) 전체 재생성이 유일한 방법 — 본문도 재생성되지만 오류 아님(내용 자체는 이미 맞는 날짜였으므로 재생성해도 유사한 정상 컨텐츠로 대체).
5. 클릭 후 로딩 스피너 확인(수 초~20여초 소요, ar_2009 실측 약 15~20초) — 스피너 사라질 때까지 대기.
6. 완료 후 재확인: 12개 항목 전부 스크롤하며 스크린샷 2회 분할(1~11번, 12번) 육안으로 제목의 날짜 문구 확인.

VERIFY:
- 12개 항목 제목 전부 목표 날짜 문구로 일치(예: "2026년 8월 25일") — 스크린샷 전체스크롤 육안, 1개라도 누락시 재시도.
- 본문 내용도 자연스러운지 확인(재생성으로 문구 자체는 바뀌지만 띠별 순서·구조는 동일해야 정상).

LIMIT/OPEN:
- 제목-only targeted 재생성 옵션의 UI존재 여부 미확정(2026-08-21 기준 미발견, 추후 버전 업데이트시 재확인 필요).
- 캐시버그의 백엔드 근본원인 미확인(youtube-top 소유, kong-bot 밖 read-only).
- "🔄 재생성" 버튼 좌표는 페이지 상태(스크롤·항목개수)에 따라 변동 — 절대 하드코딩 금지, 매 실행시 a11y 재탐색 필수.

REF: kaymaps/localhost-cinebot/RECIPE_fortune_12_generate.txt(§TITLE-DATE-CACHE, 동일 사고 기록), RECIPE_fortune_12_full_flow.txt(전체플로우 통합본).
