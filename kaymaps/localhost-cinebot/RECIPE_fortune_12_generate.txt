RECIPE: fortune_12_generate (SPLIT from fortune_12_full_flow — 운세생성 단독)
REG: localhost:4000(CineBot) 포춘 탭. STEPS 1-10 of fortune_12_full_flow.txt.
WHY: 운세생성 단계만 단독 재사용 필요시(시나리오/슬라이드 이후단계 불요) 참조.

STEPS:
1. Chrome 포커스 확인 필수(매 a11y 조회 전): `see --a11y --compact --pid <chrome_pid>` 요소수 10여개=백그라운드. 복구 `open -a "Google Chrome"` + sleep 2(가장 간단, 실증됨) 또는 Dock 아이콘 클릭.
   ★TROUBLESHOOT(2026-08-21 ar_2002 실증): 백그라운드 상태서 `kongtrol input key` 호출시 "사용자가 작업을 거부했습니다" 오류 — click은 되는데 key만 거부되는 비대칭 증상. 이건 kongtrol 권한버그 아님, ★Chrome 포커스 손실 신호. needs-info 전에 재포커스+재시도 먼저.
2. 새로고침(~95,102) → 포춘 라디오버튼 클릭(~1150,646, 매번 a11y 재확인 — 페이지 상태따라 변동가능).
3. 날짜 세팅: `see --a11y --compact | grep -i "incrementor\|날짜"`로 연/월/일 3개 AXIncrementor 좌표 확인. 클릭 후 `input key up/down --yes`로 증감(--yes 필수, 빠뜨리면 거부됨).
   값 검증: a11y value 미노출 → `screencapture -R<x>,<y-17>,300,40` zoom-crop 후 Read 육안확인(100% 신뢰).
4. 나레이터: 기본값 "괜찮은 음성- 여성 1"이면 조작 불요(AXComboBox로 확인만). 다른값이면 드롭다운 클릭→메뉴에서 여성1 선택.
5. 백엔드 ZODIAC 체크박스 기본 활성 — 별도조작 불요, 확인만.
6. "운세 생성" 버튼 클릭(`see --a11y --compact | grep "운세 생성"`로 좌표 확인, 페이지 스크롤에 따라 변동).
7. 진행관찰 = API 우선(ground truth): `curl http://localhost:15000/fortune/progress?date=<YYYY-MM-DD>&lang=ko` → `{running,current,completed[],failed[],pending[],total}`.
   Monitor 도구로 폴링 권장(수동 sleep 반복 금지) — poll interval 5s, running:false 되면 종료.
8. 15000 hang 가능성(간헐적, 20~80초): connection refused(HTTP 000) 반복시 자연회복 대기(3~4회 실증, kill/재시작은 명시승인 없이 금지).
9. completed.length=12, pending=[], failed=[] 확인시 배치종료.
   ★배너("⚠ zodiac Qwen 기동실패")는 대부분 오탐 — 배너만으로 폐기판단 금지, 12개 항목 실제 존재여부 먼저 확인.
10. 검증: 전체화면 screencapture(-x, crop없이)로 1~12번 라벨+제목 육안카운트 또는 API completed.length===12&&failed.length===0.

VERIFY:
- API `completed.length===12 && failed.length===0 && pending.length===0`.
- 결과화면 스크롤하며 쥐~돼지 12개 텍스트(제목+본문) 육안 확인.

LIMIT/OPEN:
- 15000 hang 근본원인 미확인(kong-bot 밖 소유, read-only).
- 나레이터/버튼 좌표 = 스크롤위치 의존, 하드코딩 금지.

★TITLE-DATE-CACHE(2026-08-21 ar_2009 실증, u_2009 지적):
- 증상: 날짜를 A→B로 바꾸고 운세생성해도, 완성화면의 YouTube 제목·해시태그가 이전 날짜(A)로 표기되는 캐시 현상(본문 내용은 정상적으로 B일 데이터).
- 해결: 앱내 "새로고침" 버튼 = 효과없음(시도했으나 무효, ar_2009 STEP1). "🔄 재생성" 버튼(위치는 스크롤 의존, a11y로 매번 재탐색) 클릭 = 효과있음(STEP3).
- ★주의(유저 지적, u_2009): "재생성" 버튼이 12개 항목 전체(본문+제목)를 다시 만든다. **제목만 문제일 땐 전체 재생성은 과함** — 제목만 targeted로 고치는 옵션이 UI에 있는지 먼저 a11y 탐색(`see --a11y | grep -i "제목.*재생성\|타이틀"`)해서 있으면 그것 사용. 없으면 전체 재생성이 유일한 방법(현재까지 확인된 바로는 그러함, 본문 내용도 동시 재생성되지만 오류는 아님).
- 검증: 재생성 후 12개 항목 제목 전부 스크롤 육안 확인 필수(스크린샷).

REF: kaymaps/localhost-cinebot/RECIPE_fortune_12_full_flow.txt(전체플로우 통합본, 시나리오+슬라이드 포함).
