RECIPE: slide_generate (SPLIT from fortune_12_full_flow — 슬라이드생성 단독, TENTATIVE 미완결)
REG: CineBot 시나리오탭. STEPS 11-15 of fortune_12_full_flow.txt.
WHY: 운세데이터 이미 존재(12개 완성)+시나리오 이미 생성된 상태에서 슬라이드생성만 재실행 필요시 참조.
PRECOND: 운세12개 완성 + "전체 시나리오로" 클릭까지 완료된 상태(RECIPE_fortune_12_generate.txt 선행 필요).

STEPS:
1. Chrome 포커스 확인(§공통, RECIPE_fortune_12_generate.txt STEP1 참조).
2. "전체 시나리오로" 버튼 클릭(~1572,196, 스크롤위치따라 변동 — a11y 매번 재확인) → 시나리오 탭 자동전환.
3. 전환 확인: 대본 textarea에 12개 항목([쥐띠]...[돼지띠]) 텍스트 채워짐 + 우측 1~12 슬라이드 슬롯 생성.
4. ★"전체 슬라이드 생성" 버튼 위치주의: "운세" 탭 근처 아님, ★시나리오탭 내부 씬목록(씬1~12) 패널 최상단.
   맨위로 스크롤 후 `see --a11y --compact | grep "전체 슬라이드 생성"`로 좌표 확인.
5. 클릭 → 씬1부터 순차 이미지+TTS 생성 시작.
6. 진행관찰: a11y `슬라이드 생성 중 (N/12)` 버튼 텍스트로 실시간 카운트(1개당 3~4분, 전체 40~50분). Monitor 폴링 권장.
   이 단계는 15000 hang과 무관한 별도 파이프라인으로 보임(중간 hang 미관찰).
7. ★완료판정 3중 확인 필수(단일 텍스트 신뢰 금지):
   (a) 상단 "영상 12/12" 초록배지
   (b) 버튼텍스트 "전체 슬라이드 생성"→"전체 슬라이드 재생성" 변경
   (c) 씬1/2 등 실제 이미지 썸네일 렌더링(빈배경 아님)
   a11y 씬별 "미생성" static text는 완료 이후에도 stale 잔존 가능(오탐, 3개 기준이 진짜).
8. 완료 후 "병합 + 설명 생성"(초록버튼) 활성화 확인 — 다음단계(미검증 영역).

VERIFY:
- "영상 12/12" 배지+버튼텍스트 원복+썸네일 이미지 3중 확인(a11y 단독텍스트 불신).

LIMIT/OPEN:
- STEPS 1-8 = fortune_12_full_flow.txt에서 발췌, 슬라이드생성 단독 실행(운세 재생성 없이)은 미실증.
- "병합+설명생성" 이후 단계 미검증.

REF: kaymaps/localhost-cinebot/RECIPE_fortune_12_full_flow.txt(전체플로우 통합본).
