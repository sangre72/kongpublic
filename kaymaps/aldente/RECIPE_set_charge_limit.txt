RECIPE: set_charge_limit(AlDente, macOS menubar app)
REG: a_982(2026-08-20), retrofit-compressed a_985
WHY: dashboard "충전 제한"(K7 app-label, keep-orig) field=custom AXTextField, std delete/backspace NOT working(digits just append, 100→10080 pollution repeat). Cmd+A select-all also fails in-field-scope. GATE-rule(2x-fail→switch-method) case.

STEPS:
1. menubar icon missing → Spotlight(Cmd+Space)+"AlDente"+Enter. Already resident → click icon→dropdown.
2. dropdown→"설정 열기"(K7 app-label) btn(a11y-coord)→dashboard window.
3. sidebar "충전 제어"(K7 app-label) click→right-pane "충전 제한: NN %" AXTextField exposed.
4. ★SET-VALUE(verified): click field→`input chord shift left` x2(existing val usu 2-3 digits)→full-text highlight(blue bg) confirmed→`input text "80"` overwrite→`input key return` confirm.
   - FAIL(tried, NOT working): `input key backspace`(repeat, no effect on field, buggy-append instead) / `input chord cmd a`(select-all fails, occasional side-effect) / mouse press-drag-release in-field text-select(no highlight generated, coord-sensitive).
4b. ★★PRECISE-PERCENT-ADJUST(REG a_988/989, 2026-08-20, reinforced a_1008, 2026-08-20): field=plain numeric AXTextField(NOT slider/AXIncrementor/spinbox — confirmed no +/- stepper UI exists here, text-entry-only). Digit-count-BEFORE-select is CRITICAL — miscounting by 1 = silent failure(78→780 concat, then clamp to 100).
   - PROCEDURE(STRICT ORDER, no skips):
     (i) click field once.
     (ii) ★zoom-crop FIRST to COUNT digits visually: `screencapture -R<fieldX-120>,<fieldY>,300,60` → read current value ON SCREEN(e.g. "100" = 3 digits, "78" = 2 digits). Write digit-count down(or use in same turn, do NOT defer).
     (iii) `input key right` x5 (safe overshoot — cursor clamps at field-end, no error) to GUARANTEE cursor at end-of-text regardless of where prior click landed.
     (iv) `input chord shift left` x(digit-count from step ii) to select exactly all existing digits. RULE: if digit-count=3, repeat 3 times; if 2, repeat 2 times; NO EXCEPTIONS.
     (v) ★zoom-crop-VERIFY full value highlighted(blue bg covers ALL digits, no partial). If selection is partial/wrong, STOP here and redo step (iii)+(iv).
     (vi) `input text "<target>"` overwrite. Type ONLY the target value, nothing else.
     (vii) `input key return` to confirm.
     (viii) ★★zoom-crop-VERIFY final value on screen matches target EXACTLY BEFORE declaring success. Typo/clamp(e.g. "900" auto-bounded to "100") caught here, not after close/quit.
   - ★FAILURE MODES (a_988/1005/1006 observations):
     - "100"→shift+left x2(digit-count under-count)→select only "0"→type "78"→field shows "780"(concat mid-string)→clamp to "100"(auto-bound). Root-cause: step ii skipped or miscounted.
     - Selection looks right but overwrite still concatenates: cursor was NOT truly at end. Re-verify step iii(right-arrow must reach true end).
   - ★KEY INSIGHT: this field has internal max-clamp(100) and min-clamp(0, observed) — typos(e.g."900") auto-correct on blur/re-check. This is a safety-net, NOT a substitute for correct entry. ALWAYS verify step viii before closing/quitting.
5. VERIFY-mandatory(a11y proven stale/unreliable here, ar_982 evidence): `screencapture -R<x>,<y>,300~350,60~80` zoom-crop field area→Read visual-check. digit+"%"+select-highlight(blue, right after typing=selected).
6. post-return re-verify via zoom-crop once more.
7. ★★WORKFLOW COMPLETION — ★AlDente is a RESIDENT BACKGROUND UTILITY(LSUIElement menu-bar app) whose charge-limit feature only works while the app process is alive. Therefore:
   - **DO NOT QUIT this app** — Quit(process termination) = deactivate the charge-limit sensing itself. This defeats the purpose of setting a limit in the first place.
   - **CORRECT completion = CLOSE window only(keep app alive in menubar)** — after setting the charge-limit value, close the dashboard window via traffic-light(red close btn, a11y-anchor method)(REG a_983/984, retrofit a_985): see [[close_app_dock common recipe]](kaymaps/_common/RECIPE_close_app_dock.txt) — Cmd+Tab-activate OR Dock-icon-click to bring window on-screen→confirm via a11y `AXWindow @(cx,cy)[w,h]`(NOT screenshot-pixel-math, see [[screenshot_coord_conversion]])→compute close-btn ≈ window-top-left + (20,20) offset→click. Verify: `pgrep -x AlDente` still returns a PID(process alive, EXPECTED and correct) + a11y elem-count on that window drops sharply(dashboard ~62→dropdown-only ~2-16).
   - If task explicitly says "quit the app" or "terminate": use menubar "나가기"(Quit) menu item → confirm dialog → `pgrep -x AlDente` should return EMPTY — but **note: this disables the charge-limit feature** and is typically NOT the right completion for this app.
8. leftover dropdown after close → `Escape` unreliable → click blank desktop area instead.

VERIFY:
- ★★POST-WRITE ZOOM-CROP MANDATORY: immediately after `input key return` on field, screenshot field-text area to confirm exact value written (before proceeding to quit/close). This catches input errors like 78→780(too-many-chars pasted) or value clamp(input "100" → auto-max) BEFORE falsely reporting success. Screenshot evidence REQUIRED in ar_ F: field.
- a11y AXTextField value=reference-only(seen stale/delayed, screen-visual priority) — don't rely on it.
- window-close: a11y elem-count dashboard(62)→dropdown-only(few) OR window gone from screenshot. `pgrep -x AlDente` alive=OK(menubar-resident, close!=quit).
- ★QUIT verification(STEP9): after "나가기"+"확인" menu sequence, MUST verify with `pgrep -x AlDente` — exit code 1 (empty PID) confirms true quit, exit code 0 (PID alive) means dialog was dismissed but app not terminated(common on first try, retry or use killall if needed).

LIMIT/OPEN:
- root-cause of backspace no-op in this custom field unconfirmed(focus retained visually but key-event not reaching field, suspected).
- shift+left repeat-count should match existing-digit-count(read via zoom-crop first, adjust repeat accordingly).
- Cmd+Q ignored by AlDente(no Quit-handler registered, suspected LSUIElement trait) — see common recipe for close-method detail.
