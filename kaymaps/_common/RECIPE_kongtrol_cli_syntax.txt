TITLE:kongtrol-cli-syntax
GATE:verified
WHY:u_1179(2026-08-20) worker used `kongtrol click X Y`(top-level) repeatedly->command-not-found->wrongly diagnosed as PATH/install issue(a_1074 brew-install draft, wrong). Root cause=CLI syntax, not PATH/install. Binary already built local, no install needed.

BIN_PATH:/Users/bumsuklee/git/kong-bot/kongtrol/target/release/kongtrol
NOTE:kongtrol=repo name AND the binary name inside it. Both true, not contradictory.

TOP_CMDS(kongtrol --help):
process|sys|service|schedule|app|input|device|game|see|help

INPUT_SUBCMDS(kongtrol input --help — most-used):
- input click <x> <y> --yes   (NOT top-level `click`)
- input text "<str>" --yes
- input key <special> --yes   (left/right/up/down/space/enter/esc)
- input chord <modifier> <key> --yes   (e.g. chord cmd n)
- input drag ... --yes
- input move <x> <y>
- input location   (query cursor logical coord, no click)

FLAGS:
--yes REQUIRED for DANGEROUS cmds(input/app/etc) else exit=2 "사용자가 작업을 거부했습니다"(misleading msg, actual cause=missing --yes, NOT real user permission denial).
--json for machine envelope. --dry-run to preview. --human for human-speed cursor interp+typed delay.

WRONG(never do): `kongtrol click 100 100` / `kongtrol text "foo"` -> command not found (click/text are NOT top-level).
RIGHT: `<BIN_PATH> input click 100 100 --yes`

RULE: before first use in a fresh session, run `<BIN_PATH> --help` and `<BIN_PATH> input --help` once to reconfirm subcommand tree instead of guessing from memory.

MORE_WRONG(2026-08-20 u_1181 재발견):
- `input keyboard Return` -> no such subcommand. RIGHT: `input key enter --yes` (lowercase, subcommand=key not keyboard).
- `input screenshot <file>` -> no such subcommand, kongtrol has NO pixel-screenshot save feature.
  For screen state: use `see --a11y --pid <PID>` (a11y tree, text+coords, NOT a saved image).
  PID via `pgrep -x "Google Chrome"` etc.

NEVER osascript/System Events(see [[pure-io-no-system-events]] memory) — kongtrol input/see ONLY, even when kongtrol syntax is unclear. On syntax confusion: re-run --help, do NOT fall back to osascript.

MORE_WRONG(2026-08-20 a_1097/1100, recurring pattern — worker keeps guessing capitalized/JS-style key names):
- `input key F5 --yes` -> not in supported list(left/right/up/down/space/enter/esc/cmd-n). Refresh = `input chord cmd r --yes` instead.
- `input key Escape --yes` -> WRONG case. RIGHT: `input key esc --yes` (lowercase).
- `input key Return --yes` -> WRONG name+case. RIGHT: `input key enter --yes` (lowercase, "enter" not "Return").
RULE: key names are ALWAYS lowercase short-form(esc/enter/space/left/right/up/down), never capitalized JS-KeyboardEvent-style names(Escape/Return/ArrowLeft). When a key isn't in the documented list, use `input chord` with the real shortcut instead of guessing a key name.

MORE_WRONG(2026-08-20 a_1125 worker-restart bootstrap):
- `input key return` (lowercase but wrong name) -> not in list. RIGHT: `input key enter --yes`.
- `input chord cmd Home` -> "Home" not a valid single-char chord key. "home" not in `input key` supported list either(left/right/up/down/space/enter/esc only). Scroll-reset = `input chord cmd r --yes`(refresh) ONLY, no Home-key shortcut exists in kongtrol.
FRESH-SESSION-CHECKLIST: on worker restart, before any click, run `<BIN_PATH> --version` + `<BIN_PATH> input --help` to reconfirm binary+syntax(session memory of prior corrections is lost on restart — this file is the durable source of truth, re-read it instead of re-guessing).
