RECIPE: zoom_crop_value_verify (TENTATIVE — GATE_POLICY, verified_runs=1, 1회 성공만·2회 재현 전)
REG: a_962/a_963(2026-08-19)
WHY: a11y 트리가 값을 노출 안 하는 컨트롤(AXIncrementor 날짜필드 등)+VLM(see --vl)이 세밀한 숫자/텍스트값 판독에서 신뢰불가(동일화면 반복질의에도 매번 다른 값 응답, ar_960/962 실증)인 경우, 값을 100% 확실하게 확인할 방법이 필요했음. 화면 확대크롭+육안읽기로 해결.
METHOD:
1. 대상 필드의 대략 좌표를 a11y(`see --a11y --compact`)로 확보(라벨 요소의 x,y).
2. 그 좌표 주변만 좁게 crop해서 스크린샷: `screencapture -x -R<x>,<y>,<width>,<height> <출력경로>.png` (x,y=crop 시작점, width/height=충분히 여유있게, 예 필드가 (144,867)면 `-R144,850,300,40` 처럼 필드 위아래 여유 포함).
3. Read 도구로 그 이미지를 직접 읽어 값을 육안 확인(글자 크게 확대되어 명확히 판독됨).
4. 값이 틀리면 실제 클릭+키입력(방향키 등)으로 조정 후 다시 1~3 반복해 재확인.
VERIFY(이 레시피 자체의 검증):
- ar_962에서 날짜필드(연/월/일 AXIncrementor) 3단계 실측 성공: 27→26(연도 조정), 19→21(일자 조정), 최종 26.08.21 확정 — 전부 crop 스크린샷으로 오차없이 확인됨.
WHEN-TO-USE(우선순위):
- a11y value 속성 없음(AXIncrementor 등 커스텀 위젯) → 1순위 이 방법.
- VLM binary판단(있다/없다·화면종류·1~2개카운트)이 적합한 경우 → VLM 계속 사용(더 빠름, 0.45s).
- VLM이 3개+정확카운트·세밀숫자·텍스트인용 등에 부적합한 경우(ar_942/943 근거) → 이 방법으로 대체.
- 상호보완: VLM=대략/이진 판단 우선(빠름), zoom-crop=정확한 값 확정 필요시(느리지만 100% 신뢰).
LIMIT/OPEN:
- crop 영역이 너무 좁으면 컨텍스트 부족(어떤 필드인지 혼동), 너무 넓으면 확대효과 약해짐 — 필드 하나~소그룹 단위 권장.
- 아직 1회 성공만(GATE_POLICY상 tentative), 동일 절차 재현 성공 시 verified_runs=1로 승격 후 index.json 정식등록.
