RECIPE: scroll (TENTATIVE — GATE_POLICY 미등록, verified_runs=0)
REG: a_954/u_982,u_983(2026-08-19)
STATUS: tentative — index.json 미등록. 1회 성공 후 tentative 유지, 동일세션/차회 재현 성공시에만 verified_runs=1로 승격+RECIPE 등록(GATE_POLICY 절차).
WHY: kongtrol CLI에 전용 `input scroll` 서브커맨드 없음(input --help 확인: key/chord/text/type-file/click/move/drag/run/location만 존재). 마우스휠 이벤트 미지원 — 방향키 기반 대체 필요. 브라우저(google-flow 온보딩 모달 스크롤 사례 이미 관찰됨)+네이티브 앱 공통 적용 목적.
METHOD(대체 스크롤):
1. 스크롤 대상 영역 클릭(포커스 확보) — `kongtrol input click <x> <y> --yes`.
2. Tab키로 스크롤 가능 컨테이너에 포커스 이동이 필요한 경우 `kongtrol input key tab --yes` 선행(google-flow 모달 사례: 클릭만으론 방향키가 페이지 전체를 안 움직이고 모달 내부까지 안 먹는 경우 있었음 — 케이스별 확인 필요).
3. 방향키 반복 입력으로 스크롤: down=아래로, up=위로. `kongtrol input key down --yes` N회 반복(N=스크롤량에 비례, 고정값 아님 — 콘텐츠 길이별 다름).
4. PageDown/PageUp(지원시 `kongtrol input key` 목록에 있는지 별도 확인 필요, 미확인) 또는 Space(브라우저 페이지 전체 스크롤, 포커스가 body일 때)도 후보.
VERIFY(스크롤 발생 여부 판정):
- before/after 스크린샷 비교(파일 비교 또는 픽셀 diff), 또는
- `kongtrol see --vl "스크롤 전/후 화면 내용이 바뀌었는가?"` binary판단(ar_942/943 검증: 있다/없다류 적합, 0.45s).
- a11y 트리 비교(`see --a11y --compact` 전/후 요소 좌표·목록 변화)도 보조 판단 가능(단, 캔버스 렌더링 콘텐츠는 a11y 미탐지 가능성 있음 — capcut 트랜지션 아이콘 사례처럼 VL 보완 필요할 수 있음).
LIMIT/OPEN:
- 방향키 스크롤은 포커스된 요소에 따라 반응 안 할 수 있음(브라우저 body vs 특정 스크롤 컨테이너 vs 모달 내부) — 매 앱/화면마다 포커스 확보 방법 재확인 필요, 범용 좌표/동작 아님.
- kongtrol에 실제 휠 이벤트(scroll delta) 네이티브 지원 추가되면 이 레시피는 폐기하고 그쪽으로 교체 권장(CON: 코드 변경은 별도 요청 필요, EXECUTION_POLICY SCOPE 참고).
- 현재 1회도 별도 라이브 검증 없이 기존 google-flow 레시피 관찰기록 기반으로 작성됨 — 실사용 검증 전까지 tentative 유지, 미등록.
