RECIPE: new_layer(krita)
REG: a_599/600(2026-08-17), verified 1x(전체화면 상태)
WHY: 빈 페인트 레이어 추가(Background는 항상 잠김, 직접 그리기 불가)
STEPS:
1. Krita 메뉴바 "레이어"(logical 306,20) click
2. 서브메뉴 "새 레이어"(logical 306,136) — click만으론 안 열림, click 후 반드시 input move(306,136)로 hover해야 오른쪽 서브메뉴(Add Paint Layer 등) 펼쳐짐
3. "Add Paint Layer"(logical 595,140, 서브메뉴 첫 항목) click
VERIFY: 레이어 패널에 "칠하기 레이어 N" 신규 항목 생성, 자동 선택됨(파란 배경)
PITFALL(a_597/598/599 diagnosed): 좌측 상단 "새 레이어" 단축아이콘(18,57)은 실제로 "새 문서" 생성 버튼(오인 주의). Background 레이어는 잠금 아이콘 있어 클릭해도 안 풀림(정상, 별도 레이어 필요 이유).
좌표 전제: macOS 시스템 전체화면 모드(트래픽라이트 없음) 기준. 창모드/부분전면화 상태에서는 좌표 전부 재측정 필요(RECIPE_fullscreen_apply.txt 먼저 적용).

★★DANGER(a_618, 2026-08-17): 하단상태바 우측 회전각도 필드(회전 다이얼 아이콘 옆 숫자, 예 "5.01°")를 클릭 후 `delete`키 입력 시 — 필드 편집이 아니라 **캔버스 전체가 현재 배경색으로 완전히 채워지는 재앙적 부작용** 발생(112MiB 그림이 단색으로 덮임). 원인: 클릭이 필드에 정확히 안 들어가고 캔버스 포커스 상태에서 delete가 전체 선택/채우기 단축키로 오동작한 것으로 추정. 복구=undo 10회 연타로 가능했으나 위험 큼.
FIX: 캔버스가 실수로 회전됐다면(예: 드래그 중 우연히 회전 제스처 트리거) **회전 필드/다이얼 근처 절대 클릭 금지**. 대신: (1) 회전 그대로 두고 좌표는 비회전 기준값 사용(약간의 시각오차 감수) 또는 (2) 반드시 Krita 재시작으로 이전 자동저장(회전 전 상태)까지 롤백. 필드 직접 편집 시도 금지.

★★★DANGER 재확인(a_628, 2026-08-17 재발): "다른 이름으로 저장"/"내보내기" 다이얼로그의 파일명 입력필드가 클릭 직후 실제 포커스를 못 받는 타이밍이 존재 — 이 상태에서 `delete` 키를 누르면 필드가 아닌 **캔버스가 포커스를 갖고 있어 delete가 전체 채우기(또는 유사 파괴적 동작)로 오동작**, 두 번째 재발(a_618, a_628) 확정.
★규칙: 텍스트필드 비우기 목적의 `delete` 키 반복 입력 **절대 금지**. 대안: (1) 필드가 이미 비어있으면 delete 불필요, 바로 text 입력 (2) 값이 있으면 클릭 직후 스크린샷으로 필드에 커서/포커스(파란 테두리) 있는지 반드시 확인 후에만 delete (3) 그래도 불안하면 필드 트리플클릭(전체선택 유도) 후 text 덮어쓰기 시도. delete 사고 발생시 = undo 클릭(120,57) 20~30회 연타로 복구(1회 delete=undo스택 다수 소모 가능, 10회로 부족할 수 있음 — 복구될 때까지 반복).
