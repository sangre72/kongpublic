RECIPE: import_media
REG: a_904/ar_904(2026-08-19), 갱신 a_906/ar_906(2026-08-19)
WHY: base editor 화면에서 미디어 파일을 프로젝트에 로드하는 최초 단계, 이후 타임라인 편집의 전제.
STEPS:
1. Home 화면에서 "Create project" 클릭(신규) 또는 이미 편집기 화면이면 좌측 "Import"(root_Media 하위 barImportViewBtn_) 클릭 → macOS 네이티브 파일선택 다이얼로그(title "Select a media resource") 오픈.
2. ★반드시 `kongtrol see --a11y --compact --pid <capcut_pid>` 로 다이얼로그 내부 요소를 먼저 확인 — 파일다이얼로그는 130+개 a11y 요소가 잡히며 검색창(Search: "이름:<query>")·검색결과 리스트(AXMenuItem)·Import 버튼(AXButton "Import")까지 라벨/좌표 모두 확보 가능. 스샷 없이 진행 가능(SPEED-OPT 준수).
3. 검색창(AXTextField "파일 이름:" 또는 우상단 돋보기, 논리좌표는 세션마다 재조회) 클릭 → 파일명 타이핑 → 검색범위가 "Mac" 전체면 동명파일이 여러 개 나올 수 있음(다른 프로젝트 사본 등) → ★경로 확인 필수: 항목 클릭 후 `AXList "경로"` 하위 breadcrumb(AXStaticText로 "iCloud Drive"/"데스크탑"/파일명 순서 나열)를 확인해 원하는 실제 경로(예: Desktop 루트)인지 검증 후 Import.
4. Import 클릭 → 좌측 Media 패널에 썸네일+파일명으로 등록 확인.
VERIFY: Media 패널에 임포트한 파일 썸네일 존재(원하는 파일 1개, 개수 일치), 클릭시 우측 프리뷰에 표시. ★"썸네일 있는지 없는지"(단순 유무)만 필요하면 `kongtrol see --vl`로 대체 가능(binary 적합, ar_942/943). ★단 "정확히 몇 개인지/의도한 파일과 일치하는지" 확인은 VLM 부적합(3개+ 카운트·세부식별 0/5 정확, ar_942) — 이 부분은 스샷 육안 또는 a11y 그대로 사용.

## ★사고사례(a_906, 2026-08-19) — 폴더 통째 임포트 실수
- 파일다이얼로그 리스트에서 스크롤 밖 좌표를 클릭했더니 개별 파일이 아니라 상위 폴더 자체("Desktop", 838 items)가 선택된 채 Import가 진행됨 → "Importing materials 0/5354" 진행 다이얼로그 발생.
- ★즉시 Cancel(다이얼로그 우하단 "Cancel"/"취소" 버튼)로 중단 가능. 프로젝트 Media 패널에 이미 들어간 경우 해당 카드 우클릭 → 컨텍스트 메뉴 "Delete" 클릭으로 제거(원본 파일은 참조만이라 무손상).
- PREVENT: 검색 후 결과 리스트 클릭 시 반드시 a11y로 선택된 항목명이 원하는 파일명과 정확히 일치하는지, 경로(breadcrumb)가 예상 경로인지 확인 후 Import. 대량 selected 카운트(예: "838 items selected")가 하단에 뜨면 즉시 의심하고 취소.
