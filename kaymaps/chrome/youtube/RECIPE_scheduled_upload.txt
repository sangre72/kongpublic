TITLE:youtube-scheduled-upload-fortune-batch
GATE:validated(verified_runs=12, ★ALL 12/12 scenes real success incl real youtube.com/shorts/<id> url each — 2026-08-20 batch fully complete. FULL RECIPE = kaymaps/chrome/youtube/RECIPE_scheduled_upload_VERIFIED.txt)

★SCROLL_DRIFT(2026-08-20 u_1204, 근본원인 후보 — 좌표 불안정 전반의 진짜 원인일 가능성 높음):
- 실수로 페이지 스크롤이 발생하면 CONFIRMED_COORDS 전체가 그대로 어긋난다(예약라디오 안눌림·닫기버튼 위치 다름 등 scene_07/08 다수 이슈의 근본원인 후보).
- ★매 scene 시작 전, 그리고 다이얼로그/탭 전환 후에는 반드시 `see --a11y --pid <PID>`로 스크롤 상태 확인. 의심되면 `input key home --yes` 또는 Cmd+R로 최상단 복귀 후 좌표 재사용.
- 절대좌표 재사용은 "방금 확인한 스크롤 위치가 기준과 같을 때"만 유효 — 다르면 반드시 재조회.

CONFIRMED_COORDS(ar_1098, this Chrome window/resolution — re-verify via see --a11y if window moved/resized/scrolled):
1. "만들기" @(1884,158) 클릭 1회 → 드롭다운/업로드페이지에 "동영상 업로드" 항목@(1820,204)이 보임
   ★★★NEVER CLICK "동영상 업로드"(u_1192 발견, scene_07 반복실패 진짜원인 — 이 항목 클릭=NSOpenPanel(OS 파일선택창) 오픈=드래그앤드롭 경로 이탈, ar_1061/1063에서 이미 NSOpenPanel엔 Finder이동 안 먹힘 확인됨). "만들기" 클릭만으로 뜨는 그 화면(빈 드롭존, "동영상을 드래그하여 업로드하세요")이 목표 상태. 추가 클릭 금지.
2. drag scene_0N.mp4 from Finder → 드롭존 @(1002,777) (클릭 없이 드래그만)
3. 제목 필드 @(838,354) click → Cmd+A(select-all) → paste/type title
4. "재생목록에 추가" @(700,1153) → "오늘의 운세" @(603,920) → "완료" @(924,1255)
5. "다음" @(1430,1247) ×3
6. ★"예약" @(606,700) — NOT the "공개"(public) radio button, easy to misclick → 날짜/시간 확인
7. "예약" 버튼(save) @(1430,1247) → 확인 다이얼로그에서 진짜 URL 확인
8. "닫기" @(1964,345) → 콘텐츠 리스트에서 실제 표시 재검증

TAB_SWITCH(if CineBot tab missing/lost — ar_1094 lesson):
- Cmd+Tab = APP switch, NOT browser tab switch. Do not use for tab nav.
- No 2-key-only chord shortcut works reliably for jumping to a specific existing tab by content.
- RELIABLE: `input chord cmd t --yes`(new tab) → `input chord cmd l --yes`(focus address bar) → `input text "localhost:4000" --yes` → `input key enter --yes`. Verify via `see --a11y --pid <PID>` (AXWindow title contains "CineBot").

FLOW(per scene):
1. CineBot tab(localhost:4000) — verify date=target date BEFORE anything(if wrong date shown, switch date in-page, NO refresh — data auto-loads per date, u_1131).
2. Copy button = the TITLE copy button, NOT "동영상 링크 복사"(link-copy, different button, easy to confuse — u_1091 lesson).
3. YouTube: "+ 만들기"→"동영상 업로드"→drag scene_0N.mp4 from real Finder window(NOT NSOpenPanel — NSOpenPanel has no Finder "go to folder" support, ar_1061 root cause).
4. Title field click→paste(kongtrol input chord cmd v --yes), NOT type(preserves exact copied string incl emoji).
5. Playlist="오늘의 운세".
6. "다음" 버튼 3회(세부정보→동영상요소→검토→공개상태).
7. 공개상태 화면에서 예약 날짜/시간 확인(기본 자동 제시값 검증) → 예약 클릭.
8. ★★only AFTER seeing "동영상 예약됨" dialog with a REAL youtube.com/shorts/<id> URL → close(X) → mark mapping.json status=done+that real url.
9. mapping.json 갱신 순서 뒤바꾸지 말 것: URL 확인 전 done 마킹 금지(2026-08-20 u_ orch 발견: scene_03이 done+url="...scene_03_pending"(placeholder literal string) 허위기록됨→orch가 pending으로 되돌리고 재작업 지시. 실측 없는 성공보고 = 신뢰붕괴).

★STALE_MENU_FIX(2026-08-20, scene_05~07 lesson — 반드시 준수):
- "만들기"@(1884,158) 드롭다운이 페이지가 stale하면(다른 동영상 상세 갔다오거나 삭제작업 후) 클릭해도 안 열림. 반복 클릭 = 열림/닫힘 토글로 꼬임.
- ★해결: 매 scene 업로드 시작 전 `input chord cmd r --yes`(Cmd+R 새로고침) → 5~6초 대기 → "만들기" 클릭 → "동영상 업로드"@(1820,204) 즉시 출현 → 드롭존@(1002,777).
- osascript activate 금지([[pure-io-no-system-events]]). Chrome 앞으로: `open -a "Google Chrome"`만.

★DRAFT_DELETE(초안 삭제 — a_1104 학습, 렌더링 이상 다이얼로그는 복구말고 폐기):
1. 열린 업로드 다이얼로그 종료 안 되면: Cmd+R → "경고"창(페이지 벗어나기) 뜨면 "새로고침"@(1061,483) 클릭 → 다이얼로그 폐기+페이지 리로드.
2. 콘텐츠 리스트 로딩 느림(최대 5~7초, 처음엔 헤더행만 보임). 항목 안 보이면 대기 후 재확인.
3. 해당 초안 체크박스 선택 → 상단 "추가 작업"@(725,449) → "완전삭제"메뉴@(741,482) → 확인창 "이해합니다"체크박스@(754,753) → "완전삭제"버튼@(1226,819).
4. Cmd+R 새로고침 후 항목 0건 확인.
5. ★삭제 진행 메시지("동영상을 삭제하는 중입니다. 처리되는 동안 이 화면을 종료해도 됩니다")가 뜨면 = 완료 신호, 대기 불필요. 바로 화면 종료/새로고침하고 다음 작업 진행 가능(백그라운드 처리, 안 기다려도 됨).
6. ★scene_07 최종교훈(ar_1133): 좀비 다이얼로그 만나면 삭제 절차 자체를 생략하고 곧장 새 업로드로 재시작하는 게 더 빠른 정답이었음 — 삭제는 필수 전제조건이 아니라 선택사항.

★SCENE07_RESOLVED(2026-08-20 ar_1133 최종): 좀비 다이얼로그 발견시 삭제절차(★DRAFT_DELETE)를 스킵하고 바로 새 업로드로 직행하는 게 정답이었음(유저 지시). 좀비/초안 삭제에 시간 쓰지 말고, 막히면 곧장 처음부터(만들기→드래그드롭) 재시작 우선 시도.

★SCENE07_ANOMALY(2026-08-20, 과거 이력 참고용 — 부분 원인규명 — u_1192 + ar_1109 추가발견):
- 1차원인(확정): "동영상 업로드" 메뉴항목이 아니라 "파일 선택"@(1002,832) 버튼을 눌러야 NSOpenPanel이 열림. "동영상 업로드" 메뉴@(1820,204) 클릭은 정상(드롭존만 뜸, 파일선택 버튼 안 누르면 NSOpenPanel 안 뜸).
- ★NSOpenPanel 특징 판별: 3버튼(옵션보기@(537,1084)/취소@(1552,1084)/열기@(1632,1084))이 하단에 있으면 NSOpenPanel. 이게 뒤에 숨어있으면 앞 다이얼로그의 모든 입력(닫기/esc/Cmd+R/Cmd+W)이 무반응인 "좀비"처럼 보임 → 먼저 NSOpenPanel "취소" 클릭으로 해소.
- 2차원인(가설, ar_1109 — scene_08에서도 재현): NSOpenPanel 없이 정상 드래그드롭해도 "다음"이 세부정보 탭을 못 벗어나는 경우 있음. 의심=업로드/트랜스코딩 백그라운드 처리 미완료 상태에서 "다음"을 너무 빨리 누름("검사완료" 문구는 저작권검사일 뿐 처리완료 신호 아닐 수 있음). scene_03~06은 우연히 처리 끝난 후 클릭됐을 가능성.
- FIX: "만들기" 클릭 1회 후 추가 클릭 없이 바로 드래그. 드래그 후 진행률/처리중 표시가 사라지고 완료 신호(또는 최소 30~45초 대기) 확인 후에만 "다음" 클릭.
- ★검증됨(ar_1112): 2차가설(처리대기) 확정. 45초 대기 후에도 "다음"이 한 번에 안 먹히면 계속(8회+) 반복클릭 — 처리가 끝나는 순간 자동으로 탭이 넘어감. 세부정보→동영상요소→검토→공개상태까지 이 방식으로 통과 성공(scene_08).
- ★신규막힘(scene_08, 미해결): 공개상태 도달 후 "예약" 텍스트@(606,700) 클릭이 scene_03/04와 달리 무효(15회 실패, 다른 x좌표도 실패). "예약"이 AXStaticText로만 잡힘(라벨과 실제 라디오 클릭영역이 어긋났을 가능성) — `see --a11y --json`으로 AXRadioButton role 요소를 별도로 찾아 그 좌표로 클릭 필요.
- ★★★사고 발생·절대금지(scene_08, ar_1115): "게시"버튼@(1430,1247)은 드롭다운이 아니라 **클릭 즉시 공개 게시**(비가역, 예약 아닌 지금 당장 공개). 예약 라디오가 안 눌릴 때 "혹시 이 버튼이 드롭다운일까" 시도하지 말 것 — 확인 안 된 상태에서 "게시" 버튼 클릭 절대 금지. 예약 선택 실패시 반드시 needs-info로 멈추고 게시 버튼은 건드리지 않는다. (실제 사고: scene_08=youtube.com/shorts/qXAKlgWA9OI 즉시공개됨, 8/21 예약 아님, mapping.json에 note로 기록됨)

★ZOMBIE_DIALOG(2026-08-20, 좀비 다이얼로그 = 탭 자체 종료로 해결):
- 잘못된 업로드(NSOpenPanel 경로 등)로 생긴 다이얼로그가 닫기버튼/esc/Cmd+R 전부 무반응인 "좀비" 상태가 될 수 있음(scene_07에서 재현).
- FIX: `input chord cmd w --yes`(현재 탭 종료, 다이얼로그도 함께 폐기) → `input chord cmd t --yes`(새탭) → `input chord cmd l --yes` → `input text "https://studio.youtube.com" --yes` → `input key enter --yes` → 콘텐츠 리스트에서 문제의 scene이 없는지(=폐기 성공) 확인 후 그 scene부터 재시작.
- 이 방법도 안 되면 사용자 개입 요청(needs-info) — kongtrol 자동복구 한계.

KONGTROL_SYNTAX: see [[RECIPE_kongtrol_cli_syntax]] in kaymaps/_common — no --screenshot flag, use see --a11y --pid <PID> only. chord=modifier+단일문자만(cmd r/cmd l/cmd t/cmd a/cmd v). tab/left/right/enter/esc/backspace = `input key <name>`. F5 미지원(새로고침=chord cmd r).

MAPPING_FILE:jobs/youtube-fortune-upload/mapping_YYYY-MM-DD.json — fields: n/zodiac/file/title/tags/status(pending|done)/youtube_url(real only)/scheduled.

REAL_URLS(2026-08-21 batch, ★FINAL 12/12 COMPLETE): scene_01=7zrHTz6z-oc, scene_02=CVyDQpJd4lU, scene_03=4ZT2XHmtX8Y, scene_04=xShBhL0E3rk, scene_05=Vd0MDu2f_vU, scene_06=1LRIYLY-Ynk, scene_07=5LnQHYjzuss, scene_08=qXAKlgWA9OI(즉시공개, 예약아님 — 유일한 예외), scene_09=bKR7z5fWpDI, scene_10=LR5nThLAhHI, scene_11=A_T5ZZ5cah4, scene_12=HNiMq8d2Nxk.

DAILY_REUSE: 다음날 배치는 이 job(jobs/youtube-fortune-upload/) 디렉토리에 mapping_YYYY-MM-DD.json 새로 생성 후 이 레시피+RECIPE_scheduled_upload_VERIFIED.txt 그대로 재사용. CineBot 날짜만 그날짜로 전환.
