TITLE:youtube-scheduled-upload-fortune-batch
GATE:validated(verified_runs=11 real success; scene_03 in-progress w/ BREAKTHROUGH — ar_1144: `input drag` bypasses NSOpenPanel entirely. Real flow: 만들기→동영상업로드(dropzone, NOT NSOpenPanel)→NEVER "파일 선택" btn(only that opens NSOpenPanel)→`input drag` Finder→dropzone = full automation possible. FULL=kaymaps/chrome/youtube/RECIPE_scheduled_upload_VERIFIED.txt)
NOTE:앱 UI 라벨(한글 버튼명 "만들기"/"동영상 업로드"/"예약"/"다음" 등)=a11y 매칭 필수→한글유지(k7-app-label-exception). 설명·교훈=영문압축.

★SCROLL_DRIFT(u_1204, likely root-cause of coord instability):
- accidental page scroll → ALL CONFIRMED_COORDS shift(예약 radio miss·닫기 pos differ = scene_07/08 issues root).
- ★before each scene + after dialog/tab-switch: `see --a11y --pid <PID>` verify scroll. if suspect → `input key home` or Cmd-R to top, then reuse coords.
- abs-coord reuse valid ONLY when just-verified scroll == baseline. differ → re-query.

CONFIRMED_COORDS(ar_1098, this window/res — re-verify via see --a11y if moved/resized/scrolled):
1. "만들기" @(1884,158) click ×1 → dropdown MENU appears(동영상 업로드/라이브 스트리밍/게시물 작성/새 재생목록/새로운 팟캐스트/새 강의), item "동영상 업로드"@(1820,204).
   ★★★CONTRADICTION(a_1142, UNRESOLVED): recipe claimed "만들기 click ALONE shows dropzone" but REPRO shows "만들기 click → dropdown MENU only(no dropzone)". reaching dropzone REQUIRES "동영상 업로드" click → but that opens NSOpenPanel(a_1139 incident root). pure-kongtrol CANNOT pass this(NSOpenPanel path-input=chord 2mod impossible, no Finder-nav). → this scene needs USER manual or hybrid(user picks file in NSOpenPanel) or verify `input drag` real drag-drop works(untested). NOT auto-solvable now.
   ★★★NEVER CLICK "동영상 업로드" blindly(u_1192, scene_07 fail root — opens NSOpenPanel=drag-drop path lost; ar_1061/1063 confirmed NSOpenPanel rejects Finder-nav). if reached, needs-info(don't risk 게시).
2. drag scene_0N.mp4 from Finder → dropzone @(1002,777) (drag only, no click).
   ★★★INCIDENT(a_1139): click "동영상 업로드"@(1820,204) → NSOpenPanel opens → chord cmd-shift-g FAIL(2 mod, chord=1 mod only) → text+enter picked arbitrary file(project.json non-video) → upload attempt → YouTube auto-reject "처리 중단됨"(not published, safe).
   ★FIX: ① close bad dialog via X@(1397,198)~(re-check "닫기" via see --a11y) → "처리 중단됨"=not published=safe. ② restart: Cmd-R → click "만들기" ONLY → drag-drop from Finder. ③ if NSOpenPanel open → Esc → restart.
   ★ROOT: chord=1 mod only. cmd-shift-g(2 mod) impossible → NSOpenPanel path-input impossible via kongtrol. ONLY drag-drop path.
   ★★★FOCUS(a_1141): before click "만들기", ensure Chrome frontmost. `open -a "Google Chrome"` then WAIT 2-3s(focus switch not instant) → `see --a11y` verify Chrome frontmost → click. else click goes to prev-frontmost(터미널 등) = no-op(dropdown never opens). verify dropdown opened before next step.
3. 제목 field @(838,354) click → Cmd-A(select-all) → paste/type title.
4. ★target = "선택"(dropdown BUTTON under 재생목록 heading), NOT "재생목록" label/radio(a_1144 lesson: searching "재생목록" catches wrong elem). find "선택" btn → center@(679,1051), click +14px down=@(679,1065) ×1 opens directly(NO 2-split-click, repeat=toggle jam) → "오늘의 운세" → "완료" @(924,1255). ★scroll-dependent; if not @(679,1065) re-query "선택" via see --a11y.
5. "다음" @(1430,1247) ×3.
6. ★"예약" @(606,700) — NOT "공개"(public) radio, easy misclick → verify date/time.
7. "예약" save-btn @(1430,1247) → verify real URL in confirm dialog.
8. "닫기" @(1964,345) → re-verify actual display in content list.
   ★닫기 coord unstable(2026-08-20): URL-confirm-dialog "닫기" may appear @(1964,345) or elsewhere(e.g. @1222,577) per scroll/render. re-query all "닫기" via see --a11y, cross-check `input location` or zoom-crop to identify URL-dialog's "닫기". no fixed-coord trust. multiple "닫기"(inactive notif-bar etc) may coexist — target only one near visible URL text.

TAB_SWITCH(if CineBot tab lost — ar_1094):
- Cmd-Tab=APP switch NOT tab switch. no 2-key chord jumps to tab by content.
- RELIABLE: chord cmd-t(new tab) → chord cmd-l(addr bar) → text "localhost:4000" → key enter. verify via see --a11y(AXWindow title="CineBot").

FLOW(per scene):
1. CineBot tab(localhost:4000) — verify date=target BEFORE anything(wrong date → switch in-page, NO refresh — auto-loads per date, u_1131).
2. Copy btn=TITLE copy, NOT "동영상 링크 복사"(link-copy, easy confuse — u_1091).
3. YouTube: click "만들기" ONLY(never "동영상 업로드", see INCIDENT)→drag scene_0N.mp4 from real Finder(NOT NSOpenPanel — no go-to-folder, ar_1061 root).
4. title field click→paste(chord cmd v), NOT type(preserves emoji).
5. playlist="오늘의 운세".
6. "다음" ×3(세부정보→동영상요소→검토→공개상태).
7. verify 예약 date/time(auto-suggested) → click 예약.
8. ★★ONLY after "동영상 예약됨" dialog with REAL youtube.com/shorts/<id> → close(X) → mark mapping done+real url.
9. mapping order: NO done-mark before URL verified(u_ orch: scene_03 false-marked done+url="...scene_03_pending" placeholder → orch reverted to pending+re-work. no-measure success-report=trust collapse).

★STALE_MENU_FIX(scene_05~07):
- "만들기"@(1884,158) dropdown won't open if page stale(after video-detail visit or delete). repeat click=open/close toggle jam.
- ★FIX: before each scene, `input chord cmd r`(Cmd-R) → wait 5-6s → click "만들기" ONLY(★★★never "동영상 업로드"@(1820,204), see INCIDENT) → dropzone@(1002,777) already shown → drag Finder.
- no osascript activate(pure-io-no-system-events). Chrome front: `open -a "Google Chrome"` only.

★DRAFT_DELETE(a_1104, render-broken dialog=discard not recover):
1. upload dialog won't close: Cmd-R → "경고"(leave-page) → click "새로고침"@(1061,483) → dialog discarded+reload.
2. content-list load slow(5-7s, header-only first). items absent → wait+re-check.
3. draft checkbox → top "추가 작업"@(725,449) → "완전삭제" menu@(741,482) → confirm "이해합니다" checkbox@(754,753) → "완전삭제" btn@(1226,819).
4. Cmd-R → verify 0 items.
5. ★delete-progress msg("동영상을 삭제하는 중입니다...")=done signal, no wait. close/refresh + proceed(background, no wait needed).
6. ★scene_07 lesson(ar_1133): zombie dialog → SKIP delete procedure, restart new upload directly = faster. delete=optional NOT prerequisite.
7. ★완전삭제 timing(a_1134): right after "완전삭제" confirm-click, immediate `see --a11y` shows stale list(virtual-scroll residual nodes → target still/dup present) — false-judge. ★must Cmd-R then re-query(before=1, after=0). don't judge "delete fail" from immediate re-query.
8. ★user-manual conflict(a_1137): if user eyeballing "동영상을 삭제하는 중입니다..." = user may be operating that screen directly. NO kongtrol input(click/drag conflict) — observe only, await.

★SCENE07_RESOLVED(ar_1133): zombie dialog → skip DRAFT_DELETE, go straight to new upload(user directive). don't spend time on zombie/draft delete; if stuck, restart from scratch(만들기→drag-drop) first.

★SCENE07_ANOMALY(history ref — partial root — u_1192 + ar_1109):
- cause1(history, reverted by a_1139 INCIDENT): once thought only "파일 선택"@(1002,832) btn dangerous, but a_1139 repro showed "동영상 업로드"@(1820,204) click ALSO opens NSOpenPanel(may differ per screen-state/timing). ★final(INCIDENT priority): don't click "동영상 업로드" either — drag from dropzone shown by "만들기" click alone.
- ★NSOpenPanel detect: 3 btns(옵션보기@(537,1084)/취소@(1552,1084)/열기@(1632,1084)) at bottom=NSOpenPanel. if hidden behind, front dialog's all input(닫기/esc/Cmd-R/Cmd-W) unresponsive="zombie" → first click NSOpenPanel "취소" to resolve.
- cause2(hypo, ar_1109 — scene_08 repro): even normal drag-drop, "다음" stuck on 세부정보 tab. suspect="다음" clicked too early while upload/transcode background incomplete("검사완료"=copyright-check only, not processing-done). scene_03~06 maybe clicked after processing coincidentally done.
- FIX: "만들기" click ×1 then drag immediately, no extra click. after drag, wait progress/processing indicator gone + done signal(or min 30-45s) before "다음".
- ★verified(ar_1112): cause2(processing-wait) confirmed. after 45s wait if "다음" not once → keep repeat-click(8+) — auto-advances tab moment processing done. 세부정보→동영상요소→검토→공개상태 passed this way(scene_08).
- ★new-block(scene_08, unresolved): at 공개상태, "예약" text@(606,700) click ineffective(15 fails, other x too) unlike scene_03/04. "예약" caught as AXStaticText only(label/radio-click-area misaligned) — find AXRadioButton role via see --a11y --json, click that coord.
- ★★★INCIDENT·FORBIDDEN(scene_08, ar_1115): "게시" btn@(1430,1247)=NOT dropdown, **click=instant publish**(irreversible, not schedule, publishes NOW). when 예약 radio won't click, DON'T try "maybe this btn is dropdown" — NEVER click "게시" unverified. 예약-select fail → MUST needs-info stop, don't touch 게시. (real incident: scene_08=youtube.com/shorts/qXAKlgWA9OI instant-published, not 8/21 schedule, noted in mapping.json)

★ZOMBIE_DIALOG(zombie dialog=resolve via tab-close):
- bad upload(NSOpenPanel path etc) dialog may go "zombie"(닫기/esc/Cmd-R all unresponsive, scene_07 repro).
- FIX: chord cmd-w(close tab+dialog) → chord cmd-t(new tab) → chord cmd-l → text "https://studio.youtube.com" → key enter → verify problem scene absent in content-list(=discard ok), restart from that scene.
- if this fails too → needs-info(kongtrol auto-recovery limit).

★BLOCK_SELECTION_FIX(a_1140, YT Studio list all-text blue-highlighted):
- cause: Cmd-A(select-all) during title-input applied to page body not input field. filter-URL Cmd-R alone won't clear.
- FIX: navigate to another tab(강의 등) or wrong-URL(권한오류 page) → 뒤로가기(back btn@(22,99)) ×2-3 → clean re-entry clears selection. then click Shorts tab@(472,287).
- ★channel-URL direct-input: watch channelID typo(lower-l/upper-I) → 권한오류. back-nav safest.

KONGTROL_SYNTAX: see [[RECIPE_kongtrol_cli_syntax]] in kaymaps/_common — no --screenshot flag, use see --a11y --pid <PID> only. chord=modifier+single-char ONLY(cmd r/cmd l/cmd t/cmd a/cmd v), 1 modifier max(cmd-shift-g impossible). tab/left/right/enter/esc/backspace = `input key <name>`. no F5(refresh=chord cmd r).

MAPPING_FILE:jobs/youtube-fortune-upload/mapping_YYYY-MM-DD.json — fields: n/zodiac/file/title/tags/status(pending|done)/youtube_url(real only)/scheduled.

REAL_URLS(2026-08-21 batch, ★FINAL 12/12): scene_01=7zrHTz6z-oc, scene_02=CVyDQpJd4lU, scene_03=4ZT2XHmtX8Y, scene_04=xShBhL0E3rk, scene_05=Vd0MDu2f_vU, scene_06=1LRIYLY-Ynk, scene_07=5LnQHYjzuss, scene_08=qXAKlgWA9OI(instant-publish, not schedule — only exception), scene_09=bKR7z5fWpDI, scene_10=LR5nThLAhHI, scene_11=A_T5ZZ5cah4, scene_12=HNiMq8d2Nxk.
