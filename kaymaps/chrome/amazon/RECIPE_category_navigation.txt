RECIPE: amazon_category_navigation(amazon.com, guest-mode, macOS Chrome)
REG: a_2890(2026-08-29)
WHY: shopping-automation groundwork, drill-down browse pattern(alt to search).

STEPS:
1. hamburger menu button: `AXButton "모든 카테고리 메뉴 열기" @(48,206)[65x39]` → click opens LEFT SLIDE-OUT panel(dark overlay on rest of page).
2. top-level menu structure(session example): sections = "디지털 콘텐츠 및 기기"(Prime Video/Music/Kindle/Appstore), "부서별 쇼핑"(Shopping by Department — 전자/컴퓨터/Alexa 스마트 홈/예술 및 공예 + "모두 보기" expand), "프로그램 및 기능", "도움말 및 설정".
3. ★STRUCTURAL DISTINCTION(important for click-logic): top-level department entries are `AXButton`(expands SUB-menu in-place, same panel) — e.g. `AXButton "전자" @(183,479)[365x42]`. LEAF subcategory entries are `AXLink`(navigates directly to a category-search-results page) — e.g. `AXLink "컴퓨터 및 액세서리" @(183,467)[365x42]`. Don't assume all entries behave the same — check `AXButton` vs `AXLink` type before deciding click-vs-expect-navigation.
4. sub-menu back-navigation: `AXButton "주 메뉴(으)로 돌아가기" @(183,205)[365x43]`("back to main menu") — appears at top of any expanded sub-panel.
5. leaf-category click → navigates to `amazon.com/s?i=<category-id>&bbn=<browse-node>&rh=n%3A...&language=ko&ref=nav_em__nav_desktop_sa_intl_<category-path>` — same `/s?` search-results URL pattern as keyword search(RECIPE_product_search.txt), just pre-filtered by category instead of keyword. Left sidebar's search-scope dropdown(top of page) updates to show the category name(session: "컴퓨터" replacing "모두").
6. empty-result handling: category-only(no keyword) can return "...에 검색 쿼리에 대한 결과가 없습니다"(no results for this query) if the leaf category+empty-search combo is too narrow — this is NORMAL amazon behavior, not a navigation failure. Page still shows recommendation carousels below the empty-message(real fallback content, not broken state).

VERIFY:
- URL contains `/s?i=` or `/s?...bbn=` = category-navigation succeeded.
- search-scope dropdown(near search bar) shows category name instead of "모두" = confirms category-filter applied.
- "결과가 없습니다" message ≠ error — check for recommendation-carousel fallback content to distinguish real-failure from just-empty-narrow-category.

LIMIT/OPEN:
- only 1 drill-down path tested this round(전자→컴퓨터 및 액세서리, hit empty-leaf). Other department paths not exhaustively explored.
- "모두 보기"(see-all, expand-full-department-list) link present but not clicked — deeper category tree beyond visible defaults unexplored.

★SSD-SEARCH-BUG WORKAROUND(a_2921, 2026-08-30): keyword-search for "SSD"/"Samsung SSD"/"Samsung 970 Pro" hit a real Amazon search-results-grid rendering bug(RECIPE_product_search.txt's "EMPTY-RESULTS-GRID FIX" section, a_2920) — category-browse SIDESTEPS this entirely. Verified path: hamburger menu(`AXButton "모든 카테고리 메뉴 열기"`)→"컴퓨터"(top-level AXButton)→"데이터 스토리지"(leaf AXLink) → lands on healthy results grid(6 real products, working add-to-cart, no empty-gap). Apply Samsung brand-filter checkbox(`AXLink "<Brand> 필터를 적용하여 결과 좁히기"` in left sidebar) to narrow further — tested clean: 61 results, real Samsung SSD 990 PRO/9100 PRO/T7/T9/990 EVO Plus/870 EVO all rendered with working add-to-cart. This is the RECOMMENDED path for any SSD-category product access when keyword-search hits the empty-grid bug, and is a fully legitimate real-user navigation flow(not a hack) — though note it's a DIFFERENT method from keyword-search, so if a task's constraint mandates search-box usage specifically, flag the tension rather than silently substituting.
