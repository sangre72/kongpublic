RECIPE: integrated_terminal(VS Code)
REG: a_979/980/981(2026-08-20)
★CON(다중창 위험): "New Terminal" 버튼(⌃⇧₩)이 하단패널에 노출되나, 여러 VS Code 창이 열려있으면 클릭이 의도와 다른 창(예: 기존 활성창)에 먹힐 위험 실증됨(a_980 사고: youtube-top창 터미널에 잘못 접근할 뻔함, 다행히 실피해 없었음). ★버튼 클릭 전후 반드시 `see --a11y --compact --pid <PID> | grep AXWindow`(첫줄) 확인 필수.
STEPS: 하단 TERMINAL 탭 옆 "+"(New Terminal) 버튼 클릭 → 새 터미널 세션 생성, 프롬프트 경로가 현재 워크스페이스 루트와 일치하는지 확인(다른 프로젝트 경로면 즉시 중단).
