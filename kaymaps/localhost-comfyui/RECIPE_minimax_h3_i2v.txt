RECIPE: minimax_h3_i2v (MiniMax H3 I2V via direct API submit — ★성공확정, 2026-08-26)
REG: http://127.0.0.1:8188 (ComfyUI 웹UI/API), macOS MPS. native ComfyUI-core support(no custom_node needed, comfy_extras/nodes_minimax_h3.py already ships w/ ComfyUI 0.33.0+).
WHY: Wan2.2로 배경/장면 프롬프트 반영 시도 하루종일 실패(흰배경 고정) — MiniMax H3로 전환해 해결. 단 gen시간은 Wan2.2보다 훨씬 느림(24분 vs 3분).

★★★필수 모델 3종(network-budget 큼, 총 ~30GB):
1. transformer(DiT): `models/diffusion_models/MiniMax-H3/MiniMax-H3-FL2VA-Pruned-Q3_K_M.gguf`(8.9GB, Abiray/MiniMax-H3-Pruned-GGUF repo — FL2VA=First-frame-to-Video, I2V용도와 일치)
2. text encoder: ★★★GGUF분할버전(main+mmproj) 사용 금지 — `comfy/sd.py`의 minimax 감지로직이 `visual.deepstack_merger_list`+`model.layers.49.self_attn.q_proj.weight` 두 키가 **동일 state_dict**에 있어야 인식하는데, GGUF_KREA-2 fork의 DualCLIPLoaderGGUF는 2파일을 별도 dict로 로드해 병합 안 됨(런타임에러: "the JSON object must be str, bytes or bytearray, not NoneType"). **정답=공식 단일파일**: `models/text_encoders/qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors`(15.7GB, Comfy-Org/MiniMax-H3 repo) — 표준 CLIPLoader로 로드, GGUF 로더 불필요. 크기도 더 작음(gguf분할본 19.76+1.14=20.9GB보다 작음) — 처음부터 이걸로 시작할 것.
3. VAE: `models/vae/minimax_h3_video_vae_fp16.safetensors`(5.2GB, Comfy-Org/MiniMax-H3 repo)

★API 포맷 노드 그래프(캔버스UI 없이 직접 submit 성공):
- `UnetLoaderGGUF`(unet_name=위 1번 파일)
- `CLIPLoader`(clip_name=위 2번 파일, **type="minimax"**★필수)
- `VAELoader`(vae_name=위 3번 파일)
- `LoadImage`(소스 이미지)
- `MiniMaxH3ImageToVideo`(clip, vae, prompt, width, height, length, first_frame=LoadImage출력) — length는 **17*k+5 그리드**(5,22,39,56,73,90,107,124...)로 스냅됨, 임의값 불가. positive conditioning + latent 동시출력.
- `MiniMaxH3SigmaShift`(model, shift_video=12.0 기본, shift_audio=3.0 기본) — UnetLoaderGGUF 출력을 감싸 KSampler에 연결.
- `KSampler`(model=SigmaShift출력, positive/negative=MiniMaxH3ImageToVideo의 conditioning출력 그대로 재사용해도 동작함, latent_image=같은 노드의 latent출력, steps=20 기본, cfg=4.0, sampler=euler, scheduler=simple)
- `VAEDecode` → `CreateVideo`(fps=24, MiniMax H3는 24fps 고정) → `SaveVideo`(★format 필드 필수, 안 넣으면 "missing 1 required positional argument: format" 에러 — CreateVideo 아니라 SaveVideo 쪽에 넣어야 함, 헷갈리기 쉬움).

VERIFY:
- API 응답 `node_errors`가 빈 딕셔너리인지 확인.
- 완료로그 `Prompt executed in HH:MM:SS`.
- ffprobe로 width/height/fps/duration 실측(length=22프레임@24fps → duration≈0.92s로 정확히 일치 확인됨).
- ★배경 반영 여부는 첫프레임이 아니라 **끝프레임**으로 확인할 것(2026-08-26 실측: 첫프레임=원본이미지 그대로 흰배경, 끝프레임=프롬프트 배경(고딕 성 대전당) 완전 반영 — 애니메이션 특성상 배경 전환/카메라무브가 뒷부분에 나타남).

★★★터보 LoRA(2026-08-26 확정, 필수 적용 권장): 기본 20스텝은 22프레임에 24분(1436초) 소요 — 원인은 스텝수(step-count) 자체, MPS/메모리 문제 아님(둘 다 실측확인됨). `lightx2v/Minimax-h3-Turbo`(HF repo, ★ModelTC 아님 — 검색시 흔히 혼동됨) 8-step distillation LoRA(`minimax_h3_fl2v_turbo_8step_v1.0_comfyui_bf16.safetensors`, 1.96GB, comfyui-suffix버전 사용) 적용시 467.79초(7:48)로 단축(3.07배speedup, 스텝비율 20/8=2.5배와 근접). 품질 저하 육안상 없음(배경/캐릭터 둘다 20스텝과 동등, side-by-side 비교완료).
적용법: `LoraLoaderModelOnly`노드를 UnetLoaderGGUF 출력과 MiniMaxH3SigmaShift 사이에 삽입(model 체인에 끼워넣기), steps=8로 변경. 4-step 변형(`minimax_h3_fl2v_turbo_4step_v1.1_768p_comfyui_bf16.safetensors`)도 존재 — 더 빠른 속도 필요시 시도해볼 만하나 미검증.
★이후 모든 MiniMax H3 생성은 이 터보로라 기본 적용 권장(20스텝 원본은 특별한 이유 없으면 안 씀).

LIMIT/OPEN:
- 속도(터보 적용 후): 7:48(468초), 여전히 Wan2.2(172초)보다 2.7배 느림. 트레이드오프: MiniMax=배경/장면 생성력 우수, Wan2.2=속도 우수(단 배경은 거의 원본유지, 인물클로즈업류에만 적합).
- 라이선스: MiniMax H3 community license가 한국/미국/EU/영국 자체호스팅을 **매출규모 무관하게 전면 제외**(별도 $20M 매출조항은 완전히 다른 조항, 지역제한을 완화 안 함) — 이 프로젝트는 사용자가 리스크 명시적으로 인지 후 승인하여 진행함. 재사용시 라이선스 재확인 필수.
- 스텝수(20)/CFG(4.0)는 문서 기본값 그대로 사용, 튜닝 안 해봄 — steps 낮추면(8-step turbo LoRA 존재한다는 언급 있었으나 미검증) 속도개선 여지 있음, 추후 확인 필요.
- Wan2.2는 이 세션에서 완전 삭제됨(17.7GB 회수) — 필요시 RECIPE_wan22_image_to_video_mps.txt 참고해 재설치.

REF: a_2454~2465 체인(2026-08-26), STRATEGY_canvas_learning.txt(API-direct-submit 원칙), RECIPE_krea2_gguf_t2i.txt(같은 세션 이미지생성 파트, 병행 유지중).

---

## ★★★UPDATE(2026-08-26 later, a_2526~2534): Turbo-LoRA rec REVERSED — L26-28 above SUPERSEDED

A/B-tested(same-seed/8step/448x768,only LoRA-node removed): NO-LoRA=325.88s < WITH-LoRA=538.20s(1.65x SLOWER w/LoRA). visual-quality comparable. ★prior "always-apply-turbo-LoRA" rec was based on 20step-vs-8step+LoRA compare only, NEVER tested 8step-vs-8step+LoRA(true A/B) until now — result reversed.

root-cause(inferred): LoRA's purpose="make 20-step-model viable at 8-step" — since we ALREADY run 8-step directly, LoRA only adds weight-merge/adapter-forward overhead w/ no further step-reduction benefit.

★NEW standard(confirmed): base MiniMax H3, 8-step, NO-LoRA. supersedes old L26-28 "apply turbo-LoRA by default" rec.

resolution constraint(★new-finding): MiniMax requires 32px-alignment(432x768→RuntimeError,shape-mismatch in patchify_video). Krea2 only needs 16-align(different!). fix=448x768(32-multiple). MiniMax-only gotcha, does NOT apply to image-pipeline(Krea2).

log-trap: node-validation-fail(RuntimeError) can STILL print "Prompt executed in NNs"(misleading) — always verify real output-file-exists+ffprobe, never trust completion-log alone.

file-location-trap: SaveVideo w/o subfolder-param saves to `output/` root(not `output/video/` where prior-session videos live) — check BOTH paths or cross-verify via history-API+prompt_id.

derivative-research(FYI,not-applied): no official distilled-checkpoint(MiniMax never released one), many 3rd-party LoRA/GGUF exist but same category — our "LoRA=counterproductive-at-direct-8step" finding likely generalizes, not pursuing further w/o specific reason. top-popularity=Kijai's own upload(MiniMax-H3_comfy,HF,397likes) — starting point if revisited.

REF(add): a_2526~2534(speed/LoRA A/B), ar_2530(phase-breakdown), ar_2531(A/B-definitive-result).

---

## ★★★UPDATE(2026-08-28, a_2794/2795): env-path correction + real CUDA/quant-backend limitation found(not a bug, no code fix exists)

★REG correction: real ComfyUI source = `~/ComfyUI-src`(git repo). `~/Documents/ComfyUI` is a **symlink** to `~/ComfyUI-Shared`(only contains `models/`+`custom_nodes/`+`input/`, no `comfy_extras`/core code there — don't edit files under Shared expecting them to be the real source). Server actually runs on **port 8000**(not 8188 as L2 states — verify via `ps aux|grep main.py`, port may drift between sessions).

★installed MiniMax-H3-specific custom nodes(both at `~/ComfyUI-Shared/custom_nodes/`, fresh-cloned 2026-08-28):
- `ComfyUI-MiniMaxH3-TeaCache`(step-skip speedup via output-reuse, `teacache.py` reviewed line-by-line — genuinely device-agnostic, `rel_l1()` works "on any device" per its own docstring, NOT a CUDA-only bug source).
- `ComfyUI-H3-Multishot`(large/elaborate node pack, 382KB `h3_multishot_utils.py` alone — episode-chaining/color-matching/keyframe tools). Contains 2 `"cuda" if available else "cpu"` fallback patterns(`h3_episode_tools.py:160` `_match()` color-blend helper, `h3_multishot_utils.py:1541` backend-availability print) — both are graceful CPU-fallbacks with a perf-warning, NOT hard MPS-incompatible breaks.
- `ComfyUI-GGUF`(shared/general-purpose, not MiniMax-specific).

★★★REAL finding(user's "CUDA-only" memory = correct in spirit, root cause = comfy_kitchen quantization backend, not app code):
- text encoder file `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` uses **nvfp4/awq quantization** — dequant/fast-matmul kernels for these live in the `comfy_kitchen` pip package(`~/miniconda3/lib/python3.10/site-packages/comfy_kitchen/`), which has **3 backends: cuda(compiled .so, none built for macOS), triton(no triton on macOS), eager(pure-PyTorch fallback, always available)**. Verified live via `python3 -c "import comfy_kitchen as ck; print(ck.list_backends())"` → cuda/triton both `available:False`, eager `available:True` with `dequantize_nvfp4`+`scaled_mm_nvfp4` etc IN its capability list(eager CAN do nvfp4, just unfused/slower).
- `h3_multishot_utils.py:1541` prints this exact tradeoff at model-load time: eager-mode "measured ~2x slower and ~1.6x the activation pool" vs cuda/triton fused kernels. This is the "CUDA-only" limitation — **not a missing feature/bug to patch, it's comfy_kitchen(3rd-party pip pkg) never having shipped MPS kernels for nvfp4/awq quant ops**. No local/upstream code patch exists(confirmed via `git -C ~/ComfyUI-src diff -- "*minimax*"`=empty, `git log --all` shows only upstream feature commits, no local fix commit).
- ★NOT re-discoverable as "text-encoding MPS acceleration" task — server itself IS already MPS(`curl :8000/system_stats`→`device:{name:mps,type:mps}` confirmed), this is a below-server-level quantization-kernel gap. Options if revisited: (a) accept eager-mode 2x slowdown as macOS's ceiling for THIS specific quantized checkpoint (b) find/use a non-quantized(bf16/fp16) variant of the text encoder if one exists upstream(not searched this task) (c) `--enable-triton-backend` flag won't help(triton itself unavailable on macOS, ImportError not just a disabled flag).

★★blocker(separate from above, orthogonal issue): as of 2026-08-28, `models/diffusion_models/`=EMPTY(DiT gone) and `models/text_encoders/`=only `qwen3vl_4b_fp8_scaled.safetensors`(NOT the 32b minimax variant) — verified via `curl :8000/models/diffusion_models`→`[]`. Only `vae/minimax_h3_video_vae_fp16.safetensors` survived. Matches this doc's own L34 pattern("Wan2.2는 이 세션에서 완전 삭제됨") — MiniMax's ~24.6GB(DiT 8.9GB+text-encoder 15.7GB) likely similarly cleaned up since 2026-08-26. Re-download needs explicit approval(network-budget GB-scale rule) before any further MiniMax H3 work can resume.

REF(add): a_2794(MPS-investigate, found server already-MPS+missing models), a_2795(CUDA-only code-search, found comfy_kitchen quant-backend gap, no code fix exists to apply).

---

## ★★★★UPDATE(2026-08-28 later, a_2796/2797/2798): CORRECTION — models were NEVER missing(API-check gap), GGUF dual-file merge SOLVED, working generation confirmed(141.4s)

★a_2794's "models missing" finding = WRONG(API-check limitation, not real). Real files(GGUF-quantized variant, replaces old .safetensors path from L5-8 above):
- diffusion: `~/ComfyUI-Shared/models/diffusion_models/MiniMax-H3/minimax_h3_fl2va-Q3_K_M.gguf`(15.6GB)
- text_encoder(2 files, see H3ClipLoaderAny below for how they combine): `~/ComfyUI-Shared/models/text_encoders/MiniMax-H3-encoder-Q4_K_M.gguf`(19.7GB, main) + `MiniMax-H3-encoder-mmproj-F16.gguf`(1.2GB, mmproj)
- vae: `~/ComfyUI-Shared/models/vae/minimax_h3_video_vae_fp16.safetensors`(unchanged from original recipe)
- turbo lora: `~/ComfyUI-Shared/models/loras/minimax_h3_turbo_4step_v4_fixed.safetensors`(current-standard, 4-step turbo — supersedes the earlier "NO-LoRA" finding at L40-46 above; that A/B was on the *old safetensors 8-step* setup, this is a *different* 4-step turbo variant on the *new GGUF* setup, not directly comparable, not re-tested against no-lora this round)

★★★why `/models/diffusion_models` REST API showed `[]`(empty) despite files existing: that generic endpoint doesn't recurse into subfolders(`MiniMax-H3/`) or doesn't index GGUF-custom-loader-registered models. **Real model list = check the SPECIFIC loader node's own `/object_info/<NodeName>`**, e.g. `curl :8000/object_info/UnetLoaderGGUF` correctly lists `MiniMax-H3/minimax_h3_fl2va-Q3_K_M.gguf` with full subpath. Lesson for future model-existence checks on this box: don't trust `/models/*` alone when GGUF/custom-node models are involved, cross-check via the loader's object_info.

★★★★THE ANSWER to "how do 2 GGUF text-encoder files merge"(recipe's original §2 warning about DualCLIPLoaderGGUF failing for minimax — still true, but irrelevant, a DIFFERENT node solves it):
**`H3ClipLoaderAny`** — a custom node shipped in the `ComfyUI-H3-Multishot` pack(NOT core ComfyUI-GGUF's CLIPLoaderGGUF/DualCLIPLoaderGGUF, which genuinely can't do this for type="minimax" — DualCLIPLoaderGGUF's type-list doesn't even include "minimax"). Takes `clip_name`+`type`+`mmproj_name` as 3 params in ONE node call, handles the internal dual-GGUF merge(the `visual.deepstack_merger_list`+main-transformer-weights merge that plain single-file CLIPLoaderGGUF can't do) internally. This is why core-ComfyUI grep-searches for merge logic came up empty — it's 3rd-party custom-node logic, not comfy core.

★★★FULL WORKING WORKFLOW(API-direct-POST payload, verified 2026-08-28, produced real output — copy-paste-ready reference):
```json
{
  "1": {"class_type": "UnetLoaderGGUF", "inputs": {"unet_name": "MiniMax-H3/minimax_h3_fl2va-Q3_K_M.gguf"}},
  "2": {"class_type": "H3ClipLoaderAny", "inputs": {"clip_name": "MiniMax-H3-encoder-Q4_K_M.gguf", "type": "minimax", "mmproj_name": "MiniMax-H3-encoder-mmproj-F16.gguf"}},
  "3": {"class_type": "VAELoader", "inputs": {"vae_name": "minimax_h3_video_vae_fp16.safetensors"}},
  "20": {"class_type": "LoraLoaderModelOnly", "inputs": {"model": ["1", 0], "lora_name": "minimax_h3_turbo_4step_v4_fixed.safetensors", "strength_model": 1.0}},
  "23": {"class_type": "EasyCache", "inputs": {"model": ["20", 0], "reuse_threshold": 0.3, "start_percent": 0.15, "end_percent": 0.95, "verbose": false}},
  "22": {"class_type": "MiniMaxH3SigmaShift", "inputs": {"model": ["23", 0], "shift_video": 12.0, "shift_audio": 6.0}},
  "5": {"class_type": "MiniMaxH3ImageToVideo", "inputs": {"clip": ["2", 0], "vae": ["3", 0], "prompt": "<YOUR PROMPT HERE>", "width": 384, "height": 672, "length": 22}},
  "6": {"class_type": "KSampler", "inputs": {"model": ["22", 0], "positive": ["5", 0], "negative": ["5", 0], "latent_image": ["5", 1], "seed": 42, "steps": 4, "cfg": 1.0, "sampler_name": "res_multistep", "scheduler": "simple", "denoise": 1.0}},
  "7": {"class_type": "VAEDecode", "inputs": {"samples": ["6", 0], "vae": ["3", 0]}},
  "30": {"class_type": "CreateVideo", "inputs": {"images": ["7", 0], "fps": 24.0}},
  "31": {"class_type": "SaveVideo", "inputs": {"video": ["30", 0], "filename_prefix": "minimax_h3_video_output", "format": "mp4"}}
}
```
Submit via `POST http://127.0.0.1:8000/prompt` with body `{"prompt": <above>}`. Only field to change per-generation = node `"5"`'s `"prompt"` text(and optionally `"6"`'s `"seed"`). ★node numbering(1,2,3,5,6,7,20,22,23,30,31 — non-sequential, gaps are normal/harmless, don't renumber) chain: `1→20(lora)→23(easycache)→22(sigmashift)→6(ksampler model input)`; `2+3→5(clip+vae+prompt→conditioning+latent)→6(positive/negative/latent_image)`; `6→7(vaedecode)→30(createvideo)→31(savevideo)`.

★★measured timing(this exact config, turbo-4step+EasyCache+GGUF): **141.4s**(ComfyUI's own `execution_start`→`execution_success` history timestamps, ms-precision) for a 22-frame/384x672/0.92s output. This supersedes L26/L42 timings above(those were the *old safetensors 8-step* setup) — current-standard GGUF+4step+EasyCache pipeline real-world number.

★verified via: real generation submitted(anime boy+girl walking on beach test, a_2797/2798), `node_errors={}` at submit(clean graph), `status:"success"` in history, output file(`minimax_h3_anime_beach_test_00001_.mp4`, 240KB) ffprobe-confirmed 384x672/24fps/0.9167s exact, visual content-match confirmed via last-frame extract(anime-style boy+girl hand-in-hand on beach, ocean waves, matches prompt).

REF(add): a_2796(model-files-present correction, API-vs-object_info lesson), a_2797(test-gen request), a_2798(orch found reference_working_minimax_workflow.json via history API replay, H3ClipLoaderAny identified, generation confirmed+timed).

---

## ★★★★★UPDATE(2026-08-28 later, a_2799/2801): per-stage timing breakdown + memory-spike root-cause found(NOT VAE decode, it's text-encoder model-load)

★★★per-stage timing(56-frame/2.33s clip, same turbo-4step+EasyCache+GGUF config as L60-115 above, measured via ComfyUI websocket `executing`/`progress` event wall-clock capture, NOT just total execution_start→success delta):
| stage | node(s) | duration | % of total |
|---|---|---|---|
| model-load(cached after 1st run) | 1,2,3,20,23,22 | 0s(cached) / ~25s(cold, see below) | — |
| text-encode+conditioning-prep | 5(H3ClipLoaderAny+MiniMaxH3ImageToVideo) | 24.4s | 9.2% |
| KSampler(4 turbo-steps) | 6 | 184.6s(≈46s/step avg) | 69.8% |
| VAEDecode | 7 | 54.6s | 20.6% |
| CreateVideo+SaveVideo | 30,31 | 0.8s | 0.3% |
| **TOTAL** | | **264.4s** | 100% |

★★★★ANSWERS the original a_2794 "MPS 텍스트인코딩 문제" question definitively: **text-encoding is NOT the bottleneck**(only 9.2% of wall-time). KSampler diffusion dominates(69.8%) — this is expected/inherent to 4-step turbo sampling on MPS, not a fixable "bug". If further speedup is wanted, target KSampler(already at practical step-minimum, res_multistep+turbo-lora) or investigate why VAEDecode(20.6%, node 7) is disproportionately slow for a decode-only step — possibly worth investigating VAEDecodeTiled(see below) even for speed, not just memory, if its tiled-batching happens to pipeline better on MPS.

★★★★MEMORY-SPIKE root-cause(user observed ~40GB, asked if tiled-VAE-decode needed): **peak RSS(19.02GB) occurs during text-encoder model-LOAD(node 2, loading the 19.7GB Q4_K_M GGUF text-encoder file into memory), NOT during VAEDecode**. Measured via 1s-interval `ps -o rss` sampling correlated to node-execution-window boundaries(same run as the timing table above):
- text-encode window(node 2→5 load+run): peak 19.02GB RSS
- KSampler window(node 6, both DiT 8.9GB + already-loaded text-encoder resident): peak 15.25GB RSS
- **VAEDecode window(node 7): peak only 0.91GB RSS** — VAE decode itself is NOT memory-heavy for this 56-frame/384x672 case.
- swap grew mildly during run(+1.62GB, peak used 20.16GB) — pressure present but not thrashing-level(cf worker_1.txt §COMFYUI-OVERNIGHT-LEARNING pt.6 swap-thrashing warning — this run doesn't match that crisis pattern).
- ★conclusion: the ~40GB user observed is likely **total system memory**(this ComfyUI process's ~19GB peak + macOS/other-apps overhead + possibly a concurrent second model still resident from a prior run), not a single-node runaway — frame-count(56 vs 22) does NOT meaningfully change VAEDecode memory footprint per this measurement(would need a direct 22-vs-56 frame VAEDecode-only A/B to fully confirm scaling, not done this round — the model-load cost dominates regardless of frame-count).

★VAEDecodeTiled node EXISTS(ComfyUI core) and DOES support video VAEs explicitly(`temporal_size`/`temporal_overlap` params, tooltip: "Only used for video VAEs: Amount of frames to decode at a time") — compatible drop-in replacement for node "7"(same `samples`+`vae` inputs, adds `tile_size`/`overlap`/`temporal_size`/`temporal_overlap`). **NOT applied this round** — given VAEDecode measured only 0.91GB peak(not the actual memory driver), tiled-decode would NOT solve the reported 40GB concern; would only matter for MUCH longer clips(hundreds of frames) where decode-batch memory might eventually dominate. Not recommended as a fix for the current symptom.
★H3TAEDecode(h3_tae_decode.py, H3-Multishot pack) is a DIFFERENT thing — explicitly a draft-quality/preview-only fast decoder(tiny-autoencoder approximation, own docstring: "the real VAE renders finals") — NOT a production-quality memory-saving alternative, don't substitute it for node 7 in final-output workflows.

★if the 40GB concern needs a real fix: target is unloading the text-encoder(19GB) after node 5 completes(before KSampler needs the 8.9GB DiT — ComfyUI's own memory manager may already do this via VRAM/RAM eviction between nodes depending on `--reserve-vram`/model-caching settings, not verified this round) or reducing model precision(Q4_K_M→smaller quant if one exists) rather than tiled-VAE — the encoder LOAD is the actual spike, not any decode step.

REF(add): a_2799(2sec-clip gen w/ per-stage ws-timing), a_2801(memory-spike investigation, RSS+swap sampling correlated to node windows, VAEDecodeTiled/H3TAEDecode researched+ruled-out as fixes for THIS symptom).

## ★★★★★★UPDATE(2026-08-28 later, a_2803/2807): CONFIRMED comfy-core BUG(not turbo/inherent) — text_encoder_device() excludes VRAMState.SHARED(Apple-Silicon unified-mem) → CPU fallback. PATCHED+VERIFIED.

★ROOT-CAUSE(a_2803, confirmed via source trace not inference): `~/ComfyUI-src/comfy/model_management.py` — `unet_inital_load_device()`(L1082) explicitly includes `VRAMState.SHARED` in its GPU-eligible check, but `text_encoder_device()`(L~1189) did NOT — asymmetry within comfy-core itself. `H3ClipLoaderAny` never sets `model_options["load_device"]` explicitly → inherits whatever `text_encoder_device()` returns → CPU on Apple Silicon(SHARED-classified) despite MPS being available.

★PATCH APPLIED(a_2807, user-approved option "a", local/uncommitted — separate git repo from kong-bot):
```
# file: ~/ComfyUI-src/comfy/model_management.py, text_encoder_device()
# BEFORE:
elif vram_state in (VRAMState.HIGH_VRAM, VRAMState.NORMAL_VRAM) or comfy.memory_management.aimdo_enabled:
# AFTER:
elif vram_state in (VRAMState.HIGH_VRAM, VRAMState.NORMAL_VRAM, VRAMState.SHARED) or comfy.memory_management.aimdo_enabled:
```
Mirrors existing `unet_inital_load_device()` SHARED-inclusive pattern. Only `text_encoder_device()` changed — unet/vae loader logic untouched.

★VERIFY: `venv/bin/python3 -c "import comfy.model_management as mm; print(mm.text_encoder_device())"` → now returns `mps`(was `cpu` pre-patch). Server restart clean(★MUST use `~/ComfyUI-src/venv/bin/python3 main.py`, NOT bare `python` — bare python=miniconda, lacks `gguf` pkg, breaks ComfyUI-GGUF/UnetLoaderGGUF entirely; confirmed via `lsof` on prior PID showing venv was the correct interpreter all along). Post-restart `/object_info/UnetLoaderGGUF` confirmed present(no regression).

★RE-TEST(same girl-on-bench config as a_2799, L119-129 above, seed changed to force fresh non-cached exec):
| stage | node(s) | BEFORE(cpu) | AFTER(mps) |
|---|---|---|---|
| text-encode+conditioning-prep(model-load+run) | 2+5 | 24.4s / 9.2% | 21.8s / 8.1% |
| KSampler(4 turbo-steps) | 6 | 184.6s / 69.8% | 187.7s / 69.9% |
| VAEDecode | 7 | 54.6s / 20.6% | 55.1s / 20.5% |
| **TOTAL** | | **264.4s** | **268.4s** |

★RESULT: patch VERIFIED functionally correct(text_encoder_device()→mps, no regressions, output file valid: 384x672/56frames/2.33s matches spec exactly) but **measured wall-clock speedup is marginal/within noise**(~2.6s faster on text-encode stage, totals within run-to-run variance — KSampler/VAEDecode deltas are noise, not signal). Likely cause: node2(H3ClipLoaderAny, the 19.7GB GGUF file-load) is disk-I/O-bound not compute-bound, so device(cpu vs mps) doesn't matter much for the LOAD portion; the actual text-encode FORWARD-PASS compute is a small fraction of the 21.8s. Correctness/architectural-cleanliness win(now behaves as ComfyUI intends for Apple Silicon unified-mem) more than a performance win for THIS specific bottleneck.

★RISK FLAG: local patch to `~/ComfyUI-src`(uncommitted). If this repo has auto-update/git-pull configured anywhere, a future pull will silently revert this fix — not blocking, just flag for awareness. No upstream PR filed(user didn't request one).

REF(add): a_2803(root-cause source-trace), a_2807(patch+verify+re-test, this section).

## ★★★★★★★UPDATE(2026-08-28 later, a_2814): QUALITY/SPEED TRADEOFF CONFIRMED — turbo-4step+EasyCache visibly softer than 8-step+no-EasyCache

★USER COMPLAINT(u_2813) has merit: turbo-4step(steps=4,cfg=1.0,res_multistep)+EasyCache(reuse_threshold=0.3) config used throughout a_2798/2799/2807 trades VISIBLE sharpness/detail for speed. Confirmed via matched-frame A/B(same seed+prompt, girl-on-bench, 22frames):
- **A(4-step+EasyCache)**: softer/flatter — less-distinct foliage texture, less-defined character detail(hair/clothing).
- **B(8-step, EasyCache REMOVED from chain — node20→node22 direct, bypass node23)**: visibly sharper — clearer leaf/tree texture, more natural lighting gradient, better character detail.
- Timing: A(KSampler)=135.3s vs B(KSampler)=207.8s → **B is 1.54x slower**(not 2x despite 2x steps — EasyCache's normal savings partially offset).

★RECOMMENDATION: turbo-4step+EasyCache = good for DRAFT/preview/iteration speed. For FINAL/deliverable-quality output, switch to 8+step + EasyCache removed(bypass node23, feed node20's output directly into node22's `model` input) — ~1.5x slower but meaningfully sharper. Not a bug, a real config tradeoff — document per-job which mode is wanted, don't silently default to turbo for final deliverables.

★HOW TO DISABLE EasyCache in workflow JSON: change node22("MiniMaxH3SigmaShift").inputs.model from `["23",0]` to `["20",0]`(skip EasyCache node23 entirely, feed LoRA-output directly), optionally raise node6("KSampler").inputs.steps from 4→8.

REF(add): a_2814(quality A/B, this section).

## ★★★★★★★★UPDATE(2026-08-28 later, a_2822): EasyCache CONFIRMED to specifically damage MOTION(not just sharpness) — direct cause of awkward page-turn. Unused guided-motion node found.

★EasyCache=motion-coherence bug(not just softness): isolated A/B(same seed/prompt/length=39f pageturn, EasyCache ON vs OFF+8step) — EasyCache-ON shows moving hand/page region degrading into a blurred white smear/blob mid-motion(frame-reuse applied to a REGION THAT'S ACTUALLY CHANGING = wrong-content artifact), while static background(trees/bench) stays fine both variants. EasyCache-OFF shows coherent hand+legible page/finger detail throughout. **For ANY motion-heavy shot(hand gestures, object manipulation, page-turns, etc), disable EasyCache** — not just a speed/sharpness tradeoff(cf a_2814 above), it's a correctness issue for moving regions specifically.

★UNUSED CAPABILITY: `H3Keyframes`(h3_keyframes.py, ComfyUI-H3-Multishot pack) accepts image-anchors at ANY frame position(not just first/last — generalizes stock ComfyUI's endpoint-only keyframing). This session's reference workflow is 100% text-prompt-only, never wires an anchor image for the described action. Real capability gap for precise guided-motion(e.g. anchor an image AT the mid-page-turn moment) — NOT implemented(bigger workflow-restructure, needs a prepared anchor image first), flagged as an open enhancement path if EasyCache-off alone isn't sufficient for a given shot.
  Note: `MiniMaxH3AddGuide`(a name mentioned in passing) does NOT exist in this pack — `H3Keyframes` is the actual mechanism, don't search for the other name.

REF(add): a_2822(motion-specific EasyCache A/B + H3Keyframes gap, this section).

## ★★★★★★★★★UPDATE(2026-08-29, a_2848/2852): Q2_K quant — NOT AVAILABLE in current-family repo, 2 alternate-repo downloads BOTH INCOMPATIBLE(broken GGUF), don't retry these 2 sources.

★a_2848 survey: `Abiray/MiniMax-H3-Pruned-GGUF`(the repo our current Q3_K_M diffusion file comes from) has NO Q2_K — lowest tier there IS Q3_K_M(currently installed, already the smallest in that repo-family). `joeygambino/MiniMax-H3-encoder-GGUF`(our Q4_K_M text-encoder source) also has no Q2 variant.

★a_2852 attempted 2 alternate-repo downloads(user-approved, ~19.82GB total, both size-verified exact-match post-download) — **BOTH FAILED AT RUNTIME, DON'T RETRY THESE SPECIFIC FILES**:
  - ❌`unsloth/MiniMax-H3-GGUF`'s `minimax_h3_fl2va_pruned-Q2_K.gguf`(6.72GB) → `ValueError: Unknown model architecture!`(ComfyUI-GGUF's `detect_arch()` tensor-name heuristic fails to recognize it as MiniMax H3, despite correct FL2VA-pruned family/filename match).
  - ❌`leejet/MiniMax-H3-GGUF`'s `qwen3vl_32b_minimax_h3-Q2_K_M.gguf`(13.1GB) → `ValueError: This gguf file is incompatible with llama.cpp!` — direct inspection(`gguf.GGUFReader`) shows only 3 metadata KV fields(vs working Q4_K_M's 31 fields) — genuinely malformed/stripped GGUF header, missing `general.architecture` entirely.
  - Both files were removed after confirming failure(don't re-download from these exact URLs expecting a different result — verified broken, not a config/path issue on our end).

★IF Q2_K still wanted later: try a DIFFERENT repo not yet checked(`molbal/MiniMax-H3-GGUF`, `joeygambino/MiniMax-H3-GGUF`'s fl2va/ref2va subdirs — not fully surveyed this round) OR accept Q2_K may be a fragile/undertested tier for this model's unusual audio-video-packed architecture and use Q3_K_S instead(smaller than current Q3_K_M, exists in `Abiray/MiniMax-H3-GGUF`'s non-pruned unet/ subdir at 15.57GB — note: NON-pruned family, larger than pruned-Q3_K_M's 8.9GB despite lower quant letter, different base-model size).

★USER'S "1분/60s" RECALL for a 1sec-clip(vs current ~141-268s baseline) remains UNEXPLAINED — could not test Q2 to confirm/deny as the cause. Open question, not resolved.

REF(add): a_2848(quant survey, no local Q2/Q8), a_2852(2 download attempts, both broken, this section).

## ★★★★★★★★★★UPDATE(2026-08-29, a_2855/2860): SageAttention/sol-attn = CUDA/Triton-hard-blocked(no bypass, real-attempt-confirmed). torch 2.11.0→2.13.0 upgrade = FREE ~23% speedup(applied+kept).

★SageAttention/ComfyUI-sol-attn/KJNodes-PatchSageAttention: ALL CUDA/Triton-exclusive, confirmed via BOTH doc-read(a_2855) AND real install-attempt(a_2860 — cloned sol-attn, wired+ran its `MiniMaxH3MemoryEfficientSolAttentionPatch` node in a live workflow → `RuntimeError: requires Triton`. Then `pip install triton`→**zero distribution exists for this platform on PyPI**(not a version issue, no wheel at all). `triton-metal` fallback→wheel-build failure). **DO NOT RETRY** — this is a hard platform wall on Apple Silicon, not a config/version problem.
  ★ONE node in that pack IS MPS-usable(no Triton dep): `MiniMaxH3ChunkFeedForward`(memory-chunking, `sol_attn`-independent) — loads/registers fine, untested for actual effect this round. If wanting memory-reduction(not speed) on future long-clip OOM-risk work, this is worth trying.

★★torch UPGRADE(2.11.0→**2.13.0**, torchvision→0.28.0, torchaudio stayed 2.11.0=genuinely latest-available) — APPLIED, VERIFIED, KEPT(not reverted):
  - Unlocks `flex_attention`+`torch.compile(flex_attention)` on MPS(both confirmed working via real tensor test — 2.11.0 hard-rejected MPS device, 2.13.0 doesn't).
  - **FREE SPEEDUP even WITHOUT wiring FlexAttention into the model**: same 56f config as a_2807's baseline, stock attention, torch2.13 → **206.11s** vs a_2807's **268.4s**(torch2.11) = **~23% faster from the torch-version-bump alone**(general MPS-backend improvement 2.11→2.13, unrelated to FlexAttention specifically).
  - Regression-checked: server restart clean, `UnetLoaderGGUF`/`H3ClipLoaderAny` both alive, a_2807's `text_encoder_device()`patch SURVIVED(still returns mps), real 56f generation succeeded(384x672/56f/2.33s spec-exact).
  - ★torchaudio version-skew(2.11.0 vs torch's 2.13.0) NOT audio-spot-checked this round — video-frame verified only. Flag if audio sync/quality issues appear in future MiniMax H3 jobs with audio output.

★FlexAttention WIRED-INTO-MODEL(deeper optimization, actually replacing MiniMax H3's attention forward with FlexAttention) — NOT attempted, real scope: would need a custom monkey-patch on `comfy/ldm/minimax/model.py`'s attn `forward`(L169-195), similar shape to sol-attn's own `_make_sol_attention_forward`(now have a real reference implementation to study, cf `~/ComfyUI-Shared/custom_nodes/ComfyUI-sol-attn/minimax.py`) — block-mask construction for H3's packed audio-video-text token layout is non-trivial custom work, separate REF if wanted.

REF(add): a_2855(SageAttention/sol-attn platform research), a_2860(real install-attempt confirmation + torch upgrade execution+verify, this section).

## ★★★★★★★★★★★UPDATE(2026-08-29, a_2869): CORRECTION — a_2860's "23% faster" was TURBO-config-only. Quality-mode(EasyCache-off) gets a SMALLER but real ~8.4% gain. Cite config when quoting either %.

★a_2860's 206.11s/268.4s(=23% faster) comparison used the FAST config(turbo-4step+EasyCache-ON). This REF re-tested the SLOW/quality config(EasyCache-OFF+8-step, same as a_2822's qa_D) for a genuine apples-to-apples: **BEFORE(torch2.11)=362.35s → AFTER(torch2.13, identical config/seed=40001/prompt)=332.02s = 8.4% faster**(not 23%).
  ★WHY smaller gain here(not just noise): quality-mode is more compute-bound(2x KSampler steps, no EasyCache reuse-savings) — general MPS-backend/kernel improvements(the likely torch2.11→2.13 source) help proportionally less against raw-FLOP-dominated workloads vs the turbo config's more overhead-sensitive profile.
  ★TAKEAWAY: torch2.13 upgrade is a net win for BOTH configs(no regression either way), but **always cite which config(turbo vs quality) when quoting a torch-upgrade speedup %** — the two numbers(23% vs 8.4%) are not interchangeable, this was a real apples-to-oranges catch by the user(u_2867/2868), not a computation error.

REF(add): a_2869(quality-mode apples-to-apples re-test, this section).

## ★★★★★★★★★★★★UPDATE(2026-08-29, a_2873): FlexAttention-into-model = BENCHMARK-CONFIRMED COUNTERPRODUCTIVE(4-24x SLOWER, DON'T WIRE). Refined page-turn prompt = CLEAN FIX(natural motion, no artifacts).

★FlexAttention wiring INVESTIGATED THOROUGHLY, NOT IMPLEMENTED(would be a regression, not an optimization) — MiniMax H3's attention(`comfy/ldm/minimax/model.py` L169-195) is **fully DENSE**(single packed `[text|cond|audio|video]` stream, `optimized_attention(...,mask=None)` unconditionally, zero native sparsity). Real MPS benchmark(MiniMax-H3-scale tensors, B=1,H=24,S=8000,D=128) BEFORE writing any patch code:
  - stock SDPA: 117.6ms/call
  - FlexAttention eager: 477.1ms/call(4.1x SLOWER)
  - FlexAttention `torch.compile()`'d(fully warmed, 20-call avg): **2861.5ms/call(24.3x SLOWER)**
  **DO NOT WIRE FlexAttention into MiniMax H3's dense attention** — torch2.13's MPS FlexAttention kernel is optimized for SPARSE block-mask patterns(matches the 12x-headline-number's fine print); on a fully-dense mask it pays block-mask overhead with none of the compute-skip benefit, worse than mask-free SDPA. This is architecture-specific(MiniMax H3 has no native sparsity) — re-derive fresh if the model ever gains genuine block-sparse structure, don't assume this conclusion transfers to other models.
  ★torch2.13's REAL, CONFIRMED speedup(a_2860/2869, 8.4-23% depending on config) comes from GENERAL kernel/backend improvements, UNRELATED to FlexAttention specifically — keep the torch2.13 upgrade(still a net win), just don't chase FlexAttention-wiring on top of it.

★REFINED PAGE-TURN PROMPT(fixes a_2814/a_2822's original awkward-motion complaint) — CONFIRMED via 7-frame visual check across full clip:
  `"anime style illustration, a girl sitting on a park bench, turning a page of her book with her right hand, moving the right page smoothly to the left, natural hand motion, trees and sunlight in the park"`
  Combined with the already-established EasyCache-off+8-step fix(a_2822): right hand visibly lifts/turns page mid-air(readable text on page), settles naturally left, ZERO blur/smear artifacts throughout. Explicit hand/direction/motion-quality language in the prompt + EasyCache-off together = clean result. Output: 384x672/73f/3.04s, TOTAL gen time=471.59s(quality-mode+73f, this specific config not directly comparable to shorter-length prior timings).

REF(add): a_2873(FlexAttention benchmark+decision-not-to-implement, refined-prompt validated fix, this section).

## ★★★★★★★★★★★★★UPDATE(2026-08-29, a_2879): sampler/scheduler swap — SWITCH default to uni_pc+simple(31.9% faster, quality-clean). karras scheduler = BROKEN for MiniMax H3(avoid entirely).

★KSampler(node6) sampler_name+scheduler tested(quality-mode, EasyCache-off+8-step, same 73f/seed as a_2873, torch2.13):
| sampler+scheduler | total(s) | vs old-default | verdict |
|---|---|---|---|
| res_multistep+simple(OLD DEFAULT) | 471.59 | — | clean |
| dpmpp_2m+karras | 502.98 | -6.7%(slower) | **BROKEN**(severe garbled-texture artifact, no subject) |
| dpmpp_2m_sde+karras | 340.84 | +27.7% | **BROKEN**(same artifact) |
| euler+normal | 327.11 | +30.6% | clean |
| **uni_pc+simple(★NEW DEFAULT)** | **321.17** | **+31.9%** | **clean, fastest** |

★**NEW RECOMMENDED DEFAULT for node6: `sampler_name=uni_pc`, `scheduler=simple`**(was res_multistep+simple) — 31.9% faster(321.17s vs 471.59s @73f/8-step), quality-verified clean via frame-extraction(coherent character+motion, no new artifacts).
★**★★NEVER USE `karras` scheduler with MiniMax H3★★** — BOTH dpmpp variants tested with karras produced catastrophically broken output(dense scale-like garbled texture, unrecognizable subject) despite ZERO node-error(silent quality-failure, not an exception — don't trust "ran without error"=quality-ok for this model). Root cause = scheduler incompatibility with H3's sampling wrapper, not the sampler algorithm(euler/dpmpp_2m_sde both worked fine paired with non-karras schedulers).
★euler+normal(327.11s, +30.6%) is a solid fallback if uni_pc ever shows an issue — also clean, nearly as fast.
★only 1 seed/prompt tested(same as a_2873 for comparability) — broader multi-seed validation would strengthen confidence further, but this is real measured evidence, not a guess.

REF(add): a_2879(sampler/scheduler speedtest, this section).

## ★★★★★★★★★★★★★★UPDATE(2026-08-29, a_2881): uni_pc+simple RE-CONFIRMED on full comparable job — but WARM vs COLD timing differs, don't treat all timing numbers as directly comparable.

★Full identical-config regen of a_2873's park-book scene(same prompt/seed/length=73f) w/ uni_pc+simple: **FRESH(cold-start, models reloaded)=390.60s**(17.2% faster than 471.59s baseline) vs a_2879's isolated **WARM(models already resident)=321.17s**(31.9% faster). Root cause confirmed via server log(not guessed): a full model reload(text-encoder 16.5GB+DiT 15.2GB+VAE 5.0GB) adds ~70s overhead when the server/session state is cold — a_2879's back-to-back candidate tests ran warm, this run followed a deliberate `/free` cache-clear to force honest fresh-not-cached measurement(first attempt came back 100%-`execution_cached`, discarded, this is why the clear was needed).
★**TAKEAWAY for future timing comparisons**: note whether a measurement is WARM(models resident, mid-session back-to-back tests) or COLD(fresh server start/first-job-of-session) — the two are NOT directly comparable, both are "real" numbers just under different conditions. uni_pc+simple's speedup holds in BOTH cases(17.2%-31.9% depending on warm/cold), recommendation to switch default stands.
★output: `park_book_refined_unipc_00002_.mp4`(384x672/73f/3.04s spec-exact), 5-frame visual check confirms clean/coherent, consistent w/ a_2879's quality-verdict.

REF(add): a_2881(full-job warm/cold timing distinction, this section).

## ★★★★★★★★★★★★★★★UPDATE(2026-08-29, a_2882/2883): uni_pc+simple works in TURBO-mode too(fastest config found tonight), 3-way real comparison table. Motion-caveat flagged(untested combo).

★uni_pc+simple TESTED in turbo-mode(EasyCache-ON+4-step, a_2799's original config, ONLY sampler/scheduler swapped) — carefully risk-checked(6-frame full-clip visual review, not just "did it error") since this combo was previously untested and karras had shown silent-breakage is possible. **RESULT: CLEAN**(no smearing/artifact, coherent throughout) — NOT broken like karras was. EasyCache's own log confirms `skipped 0/4 steps(1.00x speedup)` — the speed gain is genuinely from uni_pc's algorithm, not a hidden EasyCache-interaction shortcut.

★3-WAY REAL COMPARISON(56f park-bench scene, all warm-state, same prompt/seed=42):
| config | sampler+scheduler | total(s) | quality |
|---|---|---|---|
| turbo(a_2799 original) | res_multistep+simple | 264.0 | clean |
| **turbo+uni_pc(★FASTEST)** | uni_pc+simple | **147.86** | clean |
| qualitymode+uni_pc | uni_pc+simple | 427.42 | clean |

★**uni_pc+simple = universal recommendation**(both turbo AND quality mode confirmed clean+faster) — no longer quality-mode-only.

★★MOTION-CAVEAT(NOT re-tested, flag before trusting blindly on motion-heavy shots): a_2883's test scene was STATIC/LOW-MOTION(girl sitting still) — does NOT re-validate a_2822's EasyCache-vs-motion finding(hand/page smearing during ACTIVE motion, e.g. page-turns) with uni_pc specifically. a_2822's EasyCache-motion-damage issue is orthogonal to sampler choice — if turbo-mode(EasyCache-ON) is wanted for a MOTION-HEAVY shot with uni_pc, test that specific combo before trusting it; for motion-heavy shots, the existing a_2822 fix(disable EasyCache entirely) remains the safe default regardless of sampler.

REF(add): a_2882(qualitymode 56f data point), a_2883(turbo+uni_pc risk-checked+clean, 3-way table, this section).

## ★★★★★★★★★★★★★★★★UPDATE(2026-08-29, a_2884): SAVED as named presets — 2 ready-to-use workflow JSONs, sanity-verified. FAST = ★DEFAULT for regular jobs going forward.

★2 preset files saved(pure ComfyUI API-graph JSON, POST directly to `/prompt`), both real-submitted+confirmed-success(zero node_errors) this REF:
  - **`telegram_bot/orchestrator/protocol/a/preset_minimax_h3_fast.json`**(★DEFAULT) — turbo-lora+EasyCache-ON+4-step+uni_pc+simple. 147.86s/56f(a_2883), 44% faster than old turbo-baseline, quality-clean on static/low-motion.
  - **`telegram_bot/orchestrator/protocol/a/preset_minimax_h3_quality.json`** — EasyCache-OFF+8-step+uni_pc+simple. 321-471s depending on frame-count/warm-cold(a_2879/2881/2882), safe for motion-heavy/complex scenes(no EasyCache-smearing risk, node23 fully removed from graph).
  - Sidecar doc: **`telegram_bot/orchestrator/protocol/a/preset_minimax_h3_README.txt`**(usage+caveats+decision-rule, kept OUTSIDE the JSON graphs themselves — a `_preset_meta` key embedded IN the JSON was tried first and broke ComfyUI's `/prompt` API validation("missing_node_type", every top-level key is treated as a graph node) — moved metadata to a separate file instead).

★**DEFAULT RULE going forward**: use `preset_minimax_h3_fast.json` unless the shot involves meaningful motion(gestures/interactions/page-turns/object-manipulation) — then use `preset_minimax_h3_quality.json`. Edit node5.prompt/length + node6.seed per-job, everything else is the confirmed-good preset — don't modify without re-testing.

REF(add): a_2884(preset save+verify, this section).

## ★★★★★★★★★★★★★★★★★UPDATE(2026-08-29, a_2885): motion-risk guidance REFINED(nuanced, not binary) — broad/slow body motion(walk/gesture) SAFE on fast-preset, fast/localized hand-object motion still risky.

★4-clip Albedo test(preset_minimax_h3_fast, 5s/124f each): 2 original-prompt + 2 explicit-motion-cue-refined-prompt, ALL 4 clean(no smearing), INCLUDING a refined walking scene w/ genuine visible dress/hair/wing motion across the full clip.

★**REFINED motion-risk model**(supersedes a_2822's simpler "any motion=risky" framing): EasyCache-smearing risk correlates with motion TYPE/SPEED/LOCALITY, not just "is there motion":
  - **SLOW/BROAD body motion**(walking gait, garment sway, hair drift, gentle gestures) = appears SAFE on fast-preset(EasyCache-ON) — a_2885's refined-walk test showed real motion, zero artifacts.
  - **FAST/LOCALIZED hand-object-interaction motion**(page-turning, precise hand manipulation — a_2822's original finding) = STILL RISKY, unconfirmed-safe on fast-preset — that specific finding stands, not overturned.
★**PRACTICAL RULE**: default to `preset_minimax_h3_fast.json`(EasyCache-ON) even for shots with walking/gesturing/broad-body-motion — only switch to `preset_minimax_h3_quality.json`(EasyCache-OFF) for shots with fast small-object manipulation(hands+props, page-turns, precise gestures involving an item).
★Prompt-refinement lesson(mirrors a_2873): explicit motion-detail language(gait/sway/shift cues) meaningfully improves ACTUAL rendered motion vs vague prompts(original "walking" prompt barely moved; refined version showed real gait/sway) — always spell out the specific motion expected, don't rely on a single verb.

REF(add): a_2885(4-clip Albedo test, refined motion-risk model, this section).
