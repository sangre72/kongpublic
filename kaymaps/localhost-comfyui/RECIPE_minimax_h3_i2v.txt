RECIPE: minimax_h3_i2v (MiniMax H3 I2V via direct API submit — ★성공확정, 2026-08-26)
REG: http://127.0.0.1:8188 (ComfyUI 웹UI/API), macOS MPS. native ComfyUI-core support(no custom_node needed, comfy_extras/nodes_minimax_h3.py already ships w/ ComfyUI 0.33.0+).
WHY: Wan2.2로 배경/장면 프롬프트 반영 시도 하루종일 실패(흰배경 고정) — MiniMax H3로 전환해 해결. 단 gen시간은 Wan2.2보다 훨씬 느림(24분 vs 3분).

★★★필수 모델 3종(network-budget 큼, 총 ~30GB):
1. transformer(DiT): `models/diffusion_models/MiniMax-H3/MiniMax-H3-FL2VA-Pruned-Q3_K_M.gguf`(8.9GB, Abiray/MiniMax-H3-Pruned-GGUF repo — FL2VA=First-frame-to-Video, I2V용도와 일치)
2. text encoder: ★★★GGUF분할버전(main+mmproj) 사용 금지 — `comfy/sd.py`의 minimax 감지로직이 `visual.deepstack_merger_list`+`model.layers.49.self_attn.q_proj.weight` 두 키가 **동일 state_dict**에 있어야 인식하는데, GGUF_KREA-2 fork의 DualCLIPLoaderGGUF는 2파일을 별도 dict로 로드해 병합 안 됨(런타임에러: "the JSON object must be str, bytes or bytearray, not NoneType"). **정답=공식 단일파일**: `models/text_encoders/qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors`(15.7GB, Comfy-Org/MiniMax-H3 repo) — 표준 CLIPLoader로 로드, GGUF 로더 불필요. 크기도 더 작음(gguf분할본 19.76+1.14=20.9GB보다 작음) — 처음부터 이걸로 시작할 것.
3. VAE: `models/vae/minimax_h3_video_vae_fp16.safetensors`(5.2GB, Comfy-Org/MiniMax-H3 repo)

★API 포맷 노드 그래프(캔버스UI 없이 직접 submit 성공):
- `UnetLoaderGGUF`(unet_name=위 1번 파일)
- `CLIPLoader`(clip_name=위 2번 파일, **type="minimax"**★필수)
- `VAELoader`(vae_name=위 3번 파일)
- `LoadImage`(소스 이미지)
- `MiniMaxH3ImageToVideo`(clip, vae, prompt, width, height, length, first_frame=LoadImage출력) — length는 **17*k+5 그리드**(5,22,39,56,73,90,107,124...)로 스냅됨, 임의값 불가. positive conditioning + latent 동시출력.
- `MiniMaxH3SigmaShift`(model, shift_video=12.0 기본, shift_audio=3.0 기본) — UnetLoaderGGUF 출력을 감싸 KSampler에 연결.
- `KSampler`(model=SigmaShift출력, positive/negative=MiniMaxH3ImageToVideo의 conditioning출력 그대로 재사용해도 동작함, latent_image=같은 노드의 latent출력, steps=20 기본, cfg=4.0, sampler=euler, scheduler=simple)
- `VAEDecode` → `CreateVideo`(fps=24, MiniMax H3는 24fps 고정) → `SaveVideo`(★format 필드 필수, 안 넣으면 "missing 1 required positional argument: format" 에러 — CreateVideo 아니라 SaveVideo 쪽에 넣어야 함, 헷갈리기 쉬움).

VERIFY:
- API 응답 `node_errors`가 빈 딕셔너리인지 확인.
- 완료로그 `Prompt executed in HH:MM:SS`.
- ffprobe로 width/height/fps/duration 실측(length=22프레임@24fps → duration≈0.92s로 정확히 일치 확인됨).
- ★배경 반영 여부는 첫프레임이 아니라 **끝프레임**으로 확인할 것(2026-08-26 실측: 첫프레임=원본이미지 그대로 흰배경, 끝프레임=프롬프트 배경(고딕 성 대전당) 완전 반영 — 애니메이션 특성상 배경 전환/카메라무브가 뒷부분에 나타남).

★★★터보 LoRA(2026-08-26 확정, 필수 적용 권장): 기본 20스텝은 22프레임에 24분(1436초) 소요 — 원인은 스텝수(step-count) 자체, MPS/메모리 문제 아님(둘 다 실측확인됨). `lightx2v/Minimax-h3-Turbo`(HF repo, ★ModelTC 아님 — 검색시 흔히 혼동됨) 8-step distillation LoRA(`minimax_h3_fl2v_turbo_8step_v1.0_comfyui_bf16.safetensors`, 1.96GB, comfyui-suffix버전 사용) 적용시 467.79초(7:48)로 단축(3.07배speedup, 스텝비율 20/8=2.5배와 근접). 품질 저하 육안상 없음(배경/캐릭터 둘다 20스텝과 동등, side-by-side 비교완료).
적용법: `LoraLoaderModelOnly`노드를 UnetLoaderGGUF 출력과 MiniMaxH3SigmaShift 사이에 삽입(model 체인에 끼워넣기), steps=8로 변경. 4-step 변형(`minimax_h3_fl2v_turbo_4step_v1.1_768p_comfyui_bf16.safetensors`)도 존재 — 더 빠른 속도 필요시 시도해볼 만하나 미검증.
★이후 모든 MiniMax H3 생성은 이 터보로라 기본 적용 권장(20스텝 원본은 특별한 이유 없으면 안 씀).

LIMIT/OPEN:
- 속도(터보 적용 후): 7:48(468초), 여전히 Wan2.2(172초)보다 2.7배 느림. 트레이드오프: MiniMax=배경/장면 생성력 우수, Wan2.2=속도 우수(단 배경은 거의 원본유지, 인물클로즈업류에만 적합).
- 라이선스: MiniMax H3 community license가 한국/미국/EU/영국 자체호스팅을 **매출규모 무관하게 전면 제외**(별도 $20M 매출조항은 완전히 다른 조항, 지역제한을 완화 안 함) — 이 프로젝트는 사용자가 리스크 명시적으로 인지 후 승인하여 진행함. 재사용시 라이선스 재확인 필수.
- 스텝수(20)/CFG(4.0)는 문서 기본값 그대로 사용, 튜닝 안 해봄 — steps 낮추면(8-step turbo LoRA 존재한다는 언급 있었으나 미검증) 속도개선 여지 있음, 추후 확인 필요.
- Wan2.2는 이 세션에서 완전 삭제됨(17.7GB 회수) — 필요시 RECIPE_wan22_image_to_video_mps.txt 참고해 재설치.

REF: a_2454~2465 체인(2026-08-26), STRATEGY_canvas_learning.txt(API-direct-submit 원칙), RECIPE_krea2_gguf_t2i.txt(같은 세션 이미지생성 파트, 병행 유지중).

---

## ★★★UPDATE(2026-08-26 later, a_2526~2534): Turbo-LoRA rec REVERSED — L26-28 above SUPERSEDED

A/B-tested(same-seed/8step/448x768,only LoRA-node removed): NO-LoRA=325.88s < WITH-LoRA=538.20s(1.65x SLOWER w/LoRA). visual-quality comparable. ★prior "always-apply-turbo-LoRA" rec was based on 20step-vs-8step+LoRA compare only, NEVER tested 8step-vs-8step+LoRA(true A/B) until now — result reversed.

root-cause(inferred): LoRA's purpose="make 20-step-model viable at 8-step" — since we ALREADY run 8-step directly, LoRA only adds weight-merge/adapter-forward overhead w/ no further step-reduction benefit.

★NEW standard(confirmed): base MiniMax H3, 8-step, NO-LoRA. supersedes old L26-28 "apply turbo-LoRA by default" rec.

resolution constraint(★new-finding): MiniMax requires 32px-alignment(432x768→RuntimeError,shape-mismatch in patchify_video). Krea2 only needs 16-align(different!). fix=448x768(32-multiple). MiniMax-only gotcha, does NOT apply to image-pipeline(Krea2).

log-trap: node-validation-fail(RuntimeError) can STILL print "Prompt executed in NNs"(misleading) — always verify real output-file-exists+ffprobe, never trust completion-log alone.

file-location-trap: SaveVideo w/o subfolder-param saves to `output/` root(not `output/video/` where prior-session videos live) — check BOTH paths or cross-verify via history-API+prompt_id.

derivative-research(FYI,not-applied): no official distilled-checkpoint(MiniMax never released one), many 3rd-party LoRA/GGUF exist but same category — our "LoRA=counterproductive-at-direct-8step" finding likely generalizes, not pursuing further w/o specific reason. top-popularity=Kijai's own upload(MiniMax-H3_comfy,HF,397likes) — starting point if revisited.

REF(add): a_2526~2534(speed/LoRA A/B), ar_2530(phase-breakdown), ar_2531(A/B-definitive-result).

---

## ★★★UPDATE(2026-08-28, a_2794/2795): env-path correction + real CUDA/quant-backend limitation found(not a bug, no code fix exists)

★REG correction: real ComfyUI source = `~/ComfyUI-src`(git repo). `~/Documents/ComfyUI` is a **symlink** to `~/ComfyUI-Shared`(only contains `models/`+`custom_nodes/`+`input/`, no `comfy_extras`/core code there — don't edit files under Shared expecting them to be the real source). Server actually runs on **port 8000**(not 8188 as L2 states — verify via `ps aux|grep main.py`, port may drift between sessions).

★installed MiniMax-H3-specific custom nodes(both at `~/ComfyUI-Shared/custom_nodes/`, fresh-cloned 2026-08-28):
- `ComfyUI-MiniMaxH3-TeaCache`(step-skip speedup via output-reuse, `teacache.py` reviewed line-by-line — genuinely device-agnostic, `rel_l1()` works "on any device" per its own docstring, NOT a CUDA-only bug source).
- `ComfyUI-H3-Multishot`(large/elaborate node pack, 382KB `h3_multishot_utils.py` alone — episode-chaining/color-matching/keyframe tools). Contains 2 `"cuda" if available else "cpu"` fallback patterns(`h3_episode_tools.py:160` `_match()` color-blend helper, `h3_multishot_utils.py:1541` backend-availability print) — both are graceful CPU-fallbacks with a perf-warning, NOT hard MPS-incompatible breaks.
- `ComfyUI-GGUF`(shared/general-purpose, not MiniMax-specific).

★★★REAL finding(user's "CUDA-only" memory = correct in spirit, root cause = comfy_kitchen quantization backend, not app code):
- text encoder file `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` uses **nvfp4/awq quantization** — dequant/fast-matmul kernels for these live in the `comfy_kitchen` pip package(`~/miniconda3/lib/python3.10/site-packages/comfy_kitchen/`), which has **3 backends: cuda(compiled .so, none built for macOS), triton(no triton on macOS), eager(pure-PyTorch fallback, always available)**. Verified live via `python3 -c "import comfy_kitchen as ck; print(ck.list_backends())"` → cuda/triton both `available:False`, eager `available:True` with `dequantize_nvfp4`+`scaled_mm_nvfp4` etc IN its capability list(eager CAN do nvfp4, just unfused/slower).
- `h3_multishot_utils.py:1541` prints this exact tradeoff at model-load time: eager-mode "measured ~2x slower and ~1.6x the activation pool" vs cuda/triton fused kernels. This is the "CUDA-only" limitation — **not a missing feature/bug to patch, it's comfy_kitchen(3rd-party pip pkg) never having shipped MPS kernels for nvfp4/awq quant ops**. No local/upstream code patch exists(confirmed via `git -C ~/ComfyUI-src diff -- "*minimax*"`=empty, `git log --all` shows only upstream feature commits, no local fix commit).
- ★NOT re-discoverable as "text-encoding MPS acceleration" task — server itself IS already MPS(`curl :8000/system_stats`→`device:{name:mps,type:mps}` confirmed), this is a below-server-level quantization-kernel gap. Options if revisited: (a) accept eager-mode 2x slowdown as macOS's ceiling for THIS specific quantized checkpoint (b) find/use a non-quantized(bf16/fp16) variant of the text encoder if one exists upstream(not searched this task) (c) `--enable-triton-backend` flag won't help(triton itself unavailable on macOS, ImportError not just a disabled flag).

★★blocker(separate from above, orthogonal issue): as of 2026-08-28, `models/diffusion_models/`=EMPTY(DiT gone) and `models/text_encoders/`=only `qwen3vl_4b_fp8_scaled.safetensors`(NOT the 32b minimax variant) — verified via `curl :8000/models/diffusion_models`→`[]`. Only `vae/minimax_h3_video_vae_fp16.safetensors` survived. Matches this doc's own L34 pattern("Wan2.2는 이 세션에서 완전 삭제됨") — MiniMax's ~24.6GB(DiT 8.9GB+text-encoder 15.7GB) likely similarly cleaned up since 2026-08-26. Re-download needs explicit approval(network-budget GB-scale rule) before any further MiniMax H3 work can resume.

REF(add): a_2794(MPS-investigate, found server already-MPS+missing models), a_2795(CUDA-only code-search, found comfy_kitchen quant-backend gap, no code fix exists to apply).

---

## ★★★★UPDATE(2026-08-28 later, a_2796/2797/2798): CORRECTION — models were NEVER missing(API-check gap), GGUF dual-file merge SOLVED, working generation confirmed(141.4s)

★a_2794's "models missing" finding = WRONG(API-check limitation, not real). Real files(GGUF-quantized variant, replaces old .safetensors path from L5-8 above):
- diffusion: `~/ComfyUI-Shared/models/diffusion_models/MiniMax-H3/minimax_h3_fl2va-Q3_K_M.gguf`(15.6GB)
- text_encoder(2 files, see H3ClipLoaderAny below for how they combine): `~/ComfyUI-Shared/models/text_encoders/MiniMax-H3-encoder-Q4_K_M.gguf`(19.7GB, main) + `MiniMax-H3-encoder-mmproj-F16.gguf`(1.2GB, mmproj)
- vae: `~/ComfyUI-Shared/models/vae/minimax_h3_video_vae_fp16.safetensors`(unchanged from original recipe)
- turbo lora: `~/ComfyUI-Shared/models/loras/minimax_h3_turbo_4step_v4_fixed.safetensors`(current-standard, 4-step turbo — supersedes the earlier "NO-LoRA" finding at L40-46 above; that A/B was on the *old safetensors 8-step* setup, this is a *different* 4-step turbo variant on the *new GGUF* setup, not directly comparable, not re-tested against no-lora this round)

★★★why `/models/diffusion_models` REST API showed `[]`(empty) despite files existing: that generic endpoint doesn't recurse into subfolders(`MiniMax-H3/`) or doesn't index GGUF-custom-loader-registered models. **Real model list = check the SPECIFIC loader node's own `/object_info/<NodeName>`**, e.g. `curl :8000/object_info/UnetLoaderGGUF` correctly lists `MiniMax-H3/minimax_h3_fl2va-Q3_K_M.gguf` with full subpath. Lesson for future model-existence checks on this box: don't trust `/models/*` alone when GGUF/custom-node models are involved, cross-check via the loader's object_info.

★★★★THE ANSWER to "how do 2 GGUF text-encoder files merge"(recipe's original §2 warning about DualCLIPLoaderGGUF failing for minimax — still true, but irrelevant, a DIFFERENT node solves it):
**`H3ClipLoaderAny`** — a custom node shipped in the `ComfyUI-H3-Multishot` pack(NOT core ComfyUI-GGUF's CLIPLoaderGGUF/DualCLIPLoaderGGUF, which genuinely can't do this for type="minimax" — DualCLIPLoaderGGUF's type-list doesn't even include "minimax"). Takes `clip_name`+`type`+`mmproj_name` as 3 params in ONE node call, handles the internal dual-GGUF merge(the `visual.deepstack_merger_list`+main-transformer-weights merge that plain single-file CLIPLoaderGGUF can't do) internally. This is why core-ComfyUI grep-searches for merge logic came up empty — it's 3rd-party custom-node logic, not comfy core.

★★★FULL WORKING WORKFLOW(API-direct-POST payload, verified 2026-08-28, produced real output — copy-paste-ready reference):
```json
{
  "1": {"class_type": "UnetLoaderGGUF", "inputs": {"unet_name": "MiniMax-H3/minimax_h3_fl2va-Q3_K_M.gguf"}},
  "2": {"class_type": "H3ClipLoaderAny", "inputs": {"clip_name": "MiniMax-H3-encoder-Q4_K_M.gguf", "type": "minimax", "mmproj_name": "MiniMax-H3-encoder-mmproj-F16.gguf"}},
  "3": {"class_type": "VAELoader", "inputs": {"vae_name": "minimax_h3_video_vae_fp16.safetensors"}},
  "20": {"class_type": "LoraLoaderModelOnly", "inputs": {"model": ["1", 0], "lora_name": "minimax_h3_turbo_4step_v4_fixed.safetensors", "strength_model": 1.0}},
  "23": {"class_type": "EasyCache", "inputs": {"model": ["20", 0], "reuse_threshold": 0.3, "start_percent": 0.15, "end_percent": 0.95, "verbose": false}},
  "22": {"class_type": "MiniMaxH3SigmaShift", "inputs": {"model": ["23", 0], "shift_video": 12.0, "shift_audio": 6.0}},
  "5": {"class_type": "MiniMaxH3ImageToVideo", "inputs": {"clip": ["2", 0], "vae": ["3", 0], "prompt": "<YOUR PROMPT HERE>", "width": 384, "height": 672, "length": 22}},
  "6": {"class_type": "KSampler", "inputs": {"model": ["22", 0], "positive": ["5", 0], "negative": ["5", 0], "latent_image": ["5", 1], "seed": 42, "steps": 4, "cfg": 1.0, "sampler_name": "res_multistep", "scheduler": "simple", "denoise": 1.0}},
  "7": {"class_type": "VAEDecode", "inputs": {"samples": ["6", 0], "vae": ["3", 0]}},
  "30": {"class_type": "CreateVideo", "inputs": {"images": ["7", 0], "fps": 24.0}},
  "31": {"class_type": "SaveVideo", "inputs": {"video": ["30", 0], "filename_prefix": "minimax_h3_video_output", "format": "mp4"}}
}
```
Submit via `POST http://127.0.0.1:8000/prompt` with body `{"prompt": <above>}`. Only field to change per-generation = node `"5"`'s `"prompt"` text(and optionally `"6"`'s `"seed"`). ★node numbering(1,2,3,5,6,7,20,22,23,30,31 — non-sequential, gaps are normal/harmless, don't renumber) chain: `1→20(lora)→23(easycache)→22(sigmashift)→6(ksampler model input)`; `2+3→5(clip+vae+prompt→conditioning+latent)→6(positive/negative/latent_image)`; `6→7(vaedecode)→30(createvideo)→31(savevideo)`.

★★measured timing(this exact config, turbo-4step+EasyCache+GGUF): **141.4s**(ComfyUI's own `execution_start`→`execution_success` history timestamps, ms-precision) for a 22-frame/384x672/0.92s output. This supersedes L26/L42 timings above(those were the *old safetensors 8-step* setup) — current-standard GGUF+4step+EasyCache pipeline real-world number.

★verified via: real generation submitted(anime boy+girl walking on beach test, a_2797/2798), `node_errors={}` at submit(clean graph), `status:"success"` in history, output file(`minimax_h3_anime_beach_test_00001_.mp4`, 240KB) ffprobe-confirmed 384x672/24fps/0.9167s exact, visual content-match confirmed via last-frame extract(anime-style boy+girl hand-in-hand on beach, ocean waves, matches prompt).

REF(add): a_2796(model-files-present correction, API-vs-object_info lesson), a_2797(test-gen request), a_2798(orch found reference_working_minimax_workflow.json via history API replay, H3ClipLoaderAny identified, generation confirmed+timed).

---

## ★★★★★UPDATE(2026-08-28 later, a_2799/2801): per-stage timing breakdown + memory-spike root-cause found(NOT VAE decode, it's text-encoder model-load)

★★★per-stage timing(56-frame/2.33s clip, same turbo-4step+EasyCache+GGUF config as L60-115 above, measured via ComfyUI websocket `executing`/`progress` event wall-clock capture, NOT just total execution_start→success delta):
| stage | node(s) | duration | % of total |
|---|---|---|---|
| model-load(cached after 1st run) | 1,2,3,20,23,22 | 0s(cached) / ~25s(cold, see below) | — |
| text-encode+conditioning-prep | 5(H3ClipLoaderAny+MiniMaxH3ImageToVideo) | 24.4s | 9.2% |
| KSampler(4 turbo-steps) | 6 | 184.6s(≈46s/step avg) | 69.8% |
| VAEDecode | 7 | 54.6s | 20.6% |
| CreateVideo+SaveVideo | 30,31 | 0.8s | 0.3% |
| **TOTAL** | | **264.4s** | 100% |

★★★★ANSWERS the original a_2794 "MPS 텍스트인코딩 문제" question definitively: **text-encoding is NOT the bottleneck**(only 9.2% of wall-time). KSampler diffusion dominates(69.8%) — this is expected/inherent to 4-step turbo sampling on MPS, not a fixable "bug". If further speedup is wanted, target KSampler(already at practical step-minimum, res_multistep+turbo-lora) or investigate why VAEDecode(20.6%, node 7) is disproportionately slow for a decode-only step — possibly worth investigating VAEDecodeTiled(see below) even for speed, not just memory, if its tiled-batching happens to pipeline better on MPS.

★★★★MEMORY-SPIKE root-cause(user observed ~40GB, asked if tiled-VAE-decode needed): **peak RSS(19.02GB) occurs during text-encoder model-LOAD(node 2, loading the 19.7GB Q4_K_M GGUF text-encoder file into memory), NOT during VAEDecode**. Measured via 1s-interval `ps -o rss` sampling correlated to node-execution-window boundaries(same run as the timing table above):
- text-encode window(node 2→5 load+run): peak 19.02GB RSS
- KSampler window(node 6, both DiT 8.9GB + already-loaded text-encoder resident): peak 15.25GB RSS
- **VAEDecode window(node 7): peak only 0.91GB RSS** — VAE decode itself is NOT memory-heavy for this 56-frame/384x672 case.
- swap grew mildly during run(+1.62GB, peak used 20.16GB) — pressure present but not thrashing-level(cf worker_1.txt §COMFYUI-OVERNIGHT-LEARNING pt.6 swap-thrashing warning — this run doesn't match that crisis pattern).
- ★conclusion: the ~40GB user observed is likely **total system memory**(this ComfyUI process's ~19GB peak + macOS/other-apps overhead + possibly a concurrent second model still resident from a prior run), not a single-node runaway — frame-count(56 vs 22) does NOT meaningfully change VAEDecode memory footprint per this measurement(would need a direct 22-vs-56 frame VAEDecode-only A/B to fully confirm scaling, not done this round — the model-load cost dominates regardless of frame-count).

★VAEDecodeTiled node EXISTS(ComfyUI core) and DOES support video VAEs explicitly(`temporal_size`/`temporal_overlap` params, tooltip: "Only used for video VAEs: Amount of frames to decode at a time") — compatible drop-in replacement for node "7"(same `samples`+`vae` inputs, adds `tile_size`/`overlap`/`temporal_size`/`temporal_overlap`). **NOT applied this round** — given VAEDecode measured only 0.91GB peak(not the actual memory driver), tiled-decode would NOT solve the reported 40GB concern; would only matter for MUCH longer clips(hundreds of frames) where decode-batch memory might eventually dominate. Not recommended as a fix for the current symptom.
★H3TAEDecode(h3_tae_decode.py, H3-Multishot pack) is a DIFFERENT thing — explicitly a draft-quality/preview-only fast decoder(tiny-autoencoder approximation, own docstring: "the real VAE renders finals") — NOT a production-quality memory-saving alternative, don't substitute it for node 7 in final-output workflows.

★if the 40GB concern needs a real fix: target is unloading the text-encoder(19GB) after node 5 completes(before KSampler needs the 8.9GB DiT — ComfyUI's own memory manager may already do this via VRAM/RAM eviction between nodes depending on `--reserve-vram`/model-caching settings, not verified this round) or reducing model precision(Q4_K_M→smaller quant if one exists) rather than tiled-VAE — the encoder LOAD is the actual spike, not any decode step.

REF(add): a_2799(2sec-clip gen w/ per-stage ws-timing), a_2801(memory-spike investigation, RSS+swap sampling correlated to node windows, VAEDecodeTiled/H3TAEDecode researched+ruled-out as fixes for THIS symptom).
