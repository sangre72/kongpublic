@A 키노트로 'AI 전망' PPT 비주얼 작성 — 순수비전 (u_20/u_21)

★★★NO-CODING 절대제약 (user 2026-08-13 u_24): "절대 코딩으로 해결해서는 안 돼."
 ✗.key/.pptx 파일 코드생성 ✗osascript로 슬라이드 만들기 ✗고정 시퀀스 스크립트 ✗결과물 코드로 뽑기.
 = PPT는 오직 [실행중인 Keynote를 화면으로 보며 실제 클릭·타이핑·도형배치]로만 만듦. 코드 허용=화면캡처·물리입력(눈·손) 뿐. 판단·결과물=100% 비전+GUI조작. 위반시 = 작업 무효.

★★PROCEED NOW (orch, u_22 "왜 멈춰있나" 응답): 대기 말고 [지금 상태에서 이어서 진행]. 워커가 여기까지 완주함(소개팝업→계속·테마→생성·제목입력·부제 자가교정). Keynote 편집화면 상태 유지중.
 - u_21 "순수 비전으로만": ★[스샷 이미지를 실제로 보고(Read png) 요소 좌표 결정] = 이게 순수비전. osascript 0(창정보 얻으려 System Events 금지). geom방출·격자트릭 안씀. 이미 하던 "스샷보고 판단"을 osascript 없이 계속 = 순수비전 부합.
 - 코어화(a_18)는 별트랙. a_17은 [눈앞 Keynote를 순수비전으로 완주] 우선. 완전 재작성으로 멈추지 말 것.
 - 다음 할일(스스로 계획·진행): 부제/저자 채움 → 슬라이드 추가(슬라이드추가 버튼 화면서 찾아 클릭) → 'AI 전망' 본문 슬라이드 3~5장(생성AI·에이전트·산업적용·리스크·미래) 제목+불릿 타이핑 → 저장(⌘S) → 최종 스샷. 매 스텝 캡처확인.

★★u_23 확장 (텍스트만→도형·오브젝트·이미지 비주얼): "Keynote에 오브젝트·텍스트·이미지 학습내용 그대로 적용. 마우스·키보드를 비전으로 보며 직접 핸들링."
 - 텍스트 넘어 [비주얼 요소]까지: 삽입/도형 툴바 버튼 화면서 찾아 클릭→도형 추가, 미디어 버튼→이미지 삽입, 스타일 패널(색·정렬) 조작. 전부 비전으로 화면보고 좌표결정→물리 마우스/키보드.
 - AI 디자인 지식 적용: 아는 좋은 슬라이드 구성(대비·여백·아이콘/도형 강조·색테마)을 실제 배치로 구현. 텍스트 나열 금지, 비주얼하게.
 - ★드래그 필요시 actor 확장(3R): 현재 actor=press/release 분리는 있으나 [drag=press→이동→release] 미구현. 도형 위치·크기 조정에 필요하면 actor.drag(x1,y1→x2,y2) + CLI `input drag` 추가. HUMAN-ONLY-IO 유지.
 - 정직: 도형 드래그배치·이미지삽입=텍스트보다 비전좌표 정밀 필요. 되는것부터 실증, 막힘=어느 조작·왜 실측보고(needs-info).


WHY: user "키노트 실행해서 AI 전망에 대하여 ppt 자료를 비주얼하게 작성." = 실앱(Keynote) 대상, 방금확정 방향(AI가 화면보고 사람처럼 조작) 실전적용.
★DIRECTION 완화(user 2026-08-13, 최신): "사람속도 아니어도 됨·빨라도 됨. 핵심=실제 UI라 뜨거나 닫을때 action 속도차 있으니 [화면을 잘 확인만 한다면] 빠르게 OK."
 = ★관찰기반 동기화(OBSERVE-THEN-ACT)가 진짜 핵심. 보간이동·글자당딜레이 [불필요](빠른 입력 OK).
 RULE OBSERVE-THEN-ACT: 매 액션 전 [화면 캡처로 상태 실측]→[목표 UI가 실제 나타났는지 확인]→확인되면 액션. 고정 sleep 추정 금지, 화면확인으로 동기화. (앱 뜨는중·창 닫히는중 = 확인될 때까지 재캡처 폴링). 판단=AI가 화면보고(규칙solver 폐기) · HUMAN-ONLY-IO(물리 마우스/키보드, osascript 금지) · 관찰물=화면녹화+핵심스샷.
ENV: /Applications/Keynote.app 설치확인(com.apple.iWork.Keynote). 손쉬운사용·화면기록 권한 승인됨.

★★LIVE 교정 (orch 실측 2026-08-13 13:29, u_19):
 - ★osascript 전면금지 재확인: 워커가 창정보 얻으려 osascript(System Events) 반복→Automation 권한팝업 유발+원칙위반. ★osascript 0회. 창/버튼 위치=오직 화면캡처 픽셀인식.
 - ★워커가 [테마선택 다이얼로그]=정상화면인데 "판단 안됨"으로 멈춤(u_19). = 화면판단 미구현. 교정: 매 스텝 [캡처→이 화면이 뭔지 AI가 판단→적절 버튼 클릭].
 - 실측된 현재화면: Keynote "테마 선택" 창(정상). '기본 흰색' 선택됨. 우하단 [취소][생성]. → ★판단=[생성] 클릭이 정답(예외 아님, 정상 다음단계). 생성 클릭 위치는 화면인식으로 [생성] 버튼 찾아 클릭.
 - 원칙: 화면에 뜬 게 팝업/다이얼로그여도 osascript 아님, [보고 어느 버튼인지 판단→물리클릭]. 정상다이얼로그(테마선택·저장확인 등)=흐름의 일부로 진행, 진짜예외(로그인암호 등 사람만)만 orch보고.

TASK: Keynote 실행→새 프레젠테이션→'AI 전망' 주제로 비주얼한 슬라이드 여러 장 작성. 과정=AI가 화면보고 사람처럼 클릭·타이핑, 녹화.

EXP:
1 콘텐츠 'AI 전망'(한국어, 비주얼 지향) — 예시 구성(워커가 AI로 판단·조정):
 - S1 표지: "AI 전망 2026" 대제목+부제
 - S2 목차 or 핵심메시지
 - S3~ 전망 포인트(예: 생성AI 확산·에이전트화·산업적용·리스크/거버넌스·미래) 각 슬라이드 제목+불릿
 - 비주얼=Keynote 테마/레이아웃 활용(제목슬라이드·불릿레이아웃 선택). 텍스트만 나열 금지, 슬라이드 구조 살려.
2 조작 = ★OBSERVE-THEN-ACT(속도 무관, 빨라도 OK): 각 액션 전 화면캡처로 [목표 상태 확인]→확인되면 즉시 실행. 예: open-a Keynote→[창 떴는지 재캡처 폴링]→뜬 것 확인→클릭. 새슬라이드 클릭→[슬라이드 추가됐는지 확인]→다음. 보간이동·글자딜레이 불필요(빠른 입력 OK). ★고정 sleep 추정 금지=화면확인으로 동기화.
3 판단 = AI(규칙 아님): 워커가 화면 스샷 보고 "다음 뭘 클릭·입력할지" 판단(새 슬라이드 버튼 위치·텍스트칸 등 화면인식). 고정 좌표 하드코딩 지양(불가피하면 명시). ★UI가 예상과 다르면(팝업·다른상태) 화면보고 대응(사람처럼).
4 관찰물: 작성 과정 화면녹화(screencapture -v, a_16 실증) mov 1개 + 핵심순간 스샷(표지완성·슬라이드추가·타이핑중·최종). 경로출력.
5 산출: 완성된 .key 파일 저장 + 최종 슬라이드 스샷.

CON: 네이티브 Keynote(u_10 부합=네이티브앱). HUMAN-ONLY-IO. 실증(11.5A3): 실제 Keynote에 슬라이드 생기고 텍스트 든 스샷·녹화로 증명. 규칙solver/고정시퀀스 지양(user).
★현실적 긴장(정직 명시): Keynote UI=격자아님 자유GUI→locator 화면추적으로 모든버튼 찾기 어려움. 사람식 클릭만으로 복잡PPT=난이도 높음.
 - 접근: ①우선 [사람식 조작]으로 되는 만큼(새프레젠테이션·테마선택·텍스트입력) 최대 실증. ②UI버튼 화면탐지 안되는 부분=어디서 막혔나 실측(어느 버튼·왜). ③"비주얼 완성도"와 "순수 사람조작" 상충하면 orch에 보고(needs-info)—우회(osascript/파일직접생성)는 금지하고 벽을 정직히.

VERIFY(ar_17): R=Keynote에 AI전망 슬라이드 몇장 작성?(녹화·스샷·.key경로 / 막힌 UI지점) · V=사람속도 조작·AI판단·슬라이드수·텍스트반영·녹화 · F=demo/play_keynote.* · actor(a_16 재사용) · N=완성도·막힌부분·유저볼 경로
USAGE: Keynote 자동작성(AI판단+사람조작) 'AI전망' PPT / 대상=macOS Keynote / src=화면캡처→AI판단→물리입력(보간·딜레이) / 산출=.key·녹화mov·스샷 / 제약=손쉬운사용·화면기록(승인)·자유GUI 탐지한계 가능
