@A ★방향전환: AI판단 + 사람속도 실시간 동작 폐루프 (메모장 대상) (u_17/u_18/u_19)

★★DIRECTION CHANGE (user 2026-08-13, 확정): "고정방식(if solver) 의미없음·폐기. 실제 사람처럼 작업."
 = 기존 a_16(고정흐름 단계스샷) 폐기·재정의. minesweeper decide 규칙solver도 방향상 폐기대상(이건 후속 a_).
NEW MODEL:
 1) 판단 = ★AI(비전)가 화면 스샷을 실제로 보고 다음 행동 결정. if문 하드코딩 금지. (이 프로젝트=Claude Code 워커가 스샷 읽고 판단하거나, 비전LLM 호출).
 2) 동작 = ★사람속도 실시간. 커서 [보간 이동](A→B 여러 스텝 부드럽게, 순간이동 금지) + 타이핑 [한 글자씩 딜레이]. 눈에 보이게.
 3) 폐루프: [화면캡처]→[AI가 보고 판단]→[사람속도 마우스/키보드 실행]→반복.

orch 실측(현 구조): actor 딜레이 30~60ms만 있음·커서 보간 없음(순간이동)→"사람처럼" 안보임. --json 인식출력 + input 서브커맨드로 외부(AI)판단 주입 가능 = 폐루프 오케 가능.

TASK(1차 = 메모장으로 실증): AI가 화면보고 "메모 열고 글 쓰자" 판단→kongtrol이 사람속도로 커서 이동·클릭·한글자 타이핑. 과정 전부 관찰가능(스샷 다수 + 화면녹화).

EXP:
1 ★사람속도 입력 (actor.rs 확장):
 - move_smooth(x,y): 현재커서→목표 [N스텝 보간 이동](각 스텝 sleep, 총 0.5~1s 눈에 보이게). 순간 move 대체.
 - type_human(text): 한 글자마다 80~150ms 딜레이(사람 타이핑 리듬).
 - CLI 플래그 --human(속도모드) 또는 기본화.
2 ★AI판단 폐루프 (오케스트레이션):
 - demo/ai_notes_loop.sh(또는 py): [screencapture]→[스샷을 판단주체(워커/비전)에 전달]→[판단결과=다음액션]→[kongtrol input 사람속도 실행]→반복.
 - 판단은 규칙 아님: 화면 상태 보고 "다음 뭐 할지"를 AI가 결정(예: 메모앱 안열림→열기, 열림→⌘N, 빈메모→타이핑 시작).
 - ★1차엔 판단주체=워커 자신이 스샷 보고 결정해도 됨(AI가 화면보고 판단 = 실증). 고정 시퀀스 하드코딩 금지.
3 ★관찰물 둘 다: (a)화면 녹화 = `screencapture -V <sec>` 또는 화면녹화로 과정 mov. (b)단계 스샷 다수. → 유저 Q2(직접관찰/녹화) 미정이라 둘 다 남겨 커버.
4 실행완주: 실제 돌려 [커서가 천천히 움직여 메모 열고 사람처럼 타이핑하는] 녹화+스샷 생성.

CON: 네이티브·HUMAN-ONLY-IO(물리 마우스/키보드, osascript 금지). u_10. 실증(11.5A3): 녹화영상에 커서 실이동·한글자 타이핑 보여야. 고정 시퀀스/규칙 solver 금지(user 강조).
NOTE: Q2 유저답(telegram msg63: live/video/both) 오면 반영. 핵심(AI판단+사람속도)은 확정이라 선진행.

ORCH_DECISION (ar_16 needs-info 응답 — 워커 진단이 이 방향전환과 일치):
 - Q "스샷 vs 영상" → ★영상 확정(+스샷 병행). screencapture -v -V 녹화 실증됨(3s 73프레임)·ffmpeg 있음 → 폐루프/타이핑 과정 녹화.
 - Q "판단 규칙 vs AI" → ★AI 확정. 규칙 solver 폐기(user "고정방식 의미없음"). 1차 판단주체=워커가 스샷 보고 결정(=AI가 화면보고 판단 실증), 고정 시퀀스 금지.
 - ★워커 발견 결함 수정필수: ⌘N 신규메모가 [이미 열린 Notes]에서 안 먹힘(이전 내용 잔존) → 창 활성화(사람식 창클릭 포커스) 후 ⌘N 확인, 새 메모 실제 생성 검증.
 - 실행: actor human-speed(커서 보간이동·글자당 80~150ms) + 녹화래퍼로 [메모 여는 과정→새메모→사람처럼 타이핑] mov 1개 + 핵심순간 스샷 몇 장.

VERIFY(ar_16): R=AI판단 폐루프+사람속도 동작 성사?(녹화·스샷 경로, 커서 보간·타이핑 딜레이 실측)/미완원인 · V=move_smooth·type_human 동작·AI판단(규칙아님)·녹화 · F=actor.rs·demo/ai_notes_loop · N=경로·Q2반영·게임(지뢰)도 이방향 전환 후속
USAGE: AI가 화면보고 사람처럼 메모 작성(실시간 관찰) / 대상=macOS Notes / src=화면캡처→AI판단→물리입력(보간·딜레이) / 산출=녹화mov+스샷·actor 사람속도 / 제약=손쉬운사용(승인)·규칙solver 폐기
