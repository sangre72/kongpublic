[STATUS]new
[REF]1075
[CHAT_ID]8638561503
[WORKER]kongewalker

@A 1075 fix-kongtrol-cli-syntax
IN: u_1179 — "kongtrol은 프로젝트 이름인데 실행을 뭔 실행을 해". 정정: kongtrol은 브랜드명이 아니라 실제 실행가능한 CLI 바이너리 맞음(kongtrol/target/release/kongtrol). 다만 지금까지 워커가 쓴 문법(`kongtrol click 100 100`)이 애초에 틀린 CLI 문법이었음(그런 서브커맨드 없음) — 그래서 "command not found"류 에러가 반복된 것. brew/PATH 문제가 아니라 **문법 오류**가 근본원인.
RULING: PATH 설치 시도(a_1074) 전면 폐기. 대신 올바른 문법으로 직접 절대경로 실행.
TASK: 1) 올바른 문법 확인됨:
  $ /Users/bumsuklee/git/kong-bot/kongtrol/target/release/kongtrol input click <X> <Y> --yes
  (click은 최상위 명령이 아니라 `input` 서브커맨드 하위. `kongtrol --help`/`kongtrol input --help`로 직접 재확인 후 사용)
  2) 테스트: 임의 안전좌표로 click 1회 실행 → exit code 확인 → 스크린샷 증거.
  3) 성공하면 scene_03 작업 재개: 포춘탭 3번 복사버튼 클릭(`kongtrol input click X Y --yes`)→YouTube 전환→만들기→업로드→드래그앤드롭→제목붙여넣기→재생목록→예약→닫기→mapping_2026-08-21.json 갱신.
  4) ★FIX-THEN-RECIPE-SYNC: 이 CLI 문법 실수를 kaymaps/_common/RECIPE_kongtrol_cli_syntax.txt(신규)에 기록 — "click은 `input click`, 텍스트는 `input text`, 조합키는 `input chord`, 최상위에 click/text/key 없음. --yes 필수. 매번 --help로 확인 후 쓰지 말고 이 레시피 먼저 참조."
CON: kongtrol이 프로젝트 자체(리포명)이면서 동시에 그 안의 실행가능 CLI 바이너리 이름이기도 함(혼동 주의, 실행 자체는 정상).
OUT: ar_1075 done(올바른 문법으로 scene_03 진행+레시피 신규작성)|needs-info(문법 재확인해도 실패시 정확한 에러)
